# W488-W493 hosted regression repairs

Group888 run35914415313 at3de1cfe2536f7fa6141ab3901cd2023d4224a114
is admitted with888 executed,884 passed,4 failed and0 missing terminals.
All176 source/input bindings matched exact Git blobs. Both new Buddy delegation
tests pass. The failures are CLI inventory drift, stale generated CLI reference,
and the two existing post-provider Chat regressions.

## Chat producer and desktop capture

W488 retains the intentionally opaque post-mint error and verifies its actual
PostProviderCall hook cause through the drained WAL. Secret, no-output, chunk,
replacement and digest assertions remain. W489 starts the real runtime through
preflight, request-bound consent and start. A semaphore holds the provider until
both bounded attach headers arrive. Full-history replay starts at cursor0;
actual exchange bounds remain distinct. Every reviewed capture failure drains
the runtime/WAL and bounds or aborts attachment tasks.

GUI146 run35914017454 at03456722 failed compilation before any test execution:
E0425 identified a cfg(test)-only consent grant helper used from the core compiled
as a GUI dependency. That is compilation evidence, not a failed GUI behavior test.
The shared capture now uses the existing prepared consent transaction API.

## Clippy and CLI surface drift

Core9abb run35911452930 passed slim Clippy and core test-target compilation;
workspace Clippy reported23 GUI-source diagnostics. Core034 run35914028695
also stopped at the same cfg(test) consent-helper compilation error. Sixteen fixes in main.rs
remove obsolete test helpers, avoid owned comparisons, initialize the test snapshot
in one expression and combine equivalent timer exits. Seven citation_gui.rs fixes
remove constant assertions and use equivalent contains checks. Independent static
review passed; hosted workspace Clippy must still run on the repaired source.

The nine real buddy cluster task-delegate leaves are now represented in the
operation inventory and its expected-gap guard. GUI dispatches the canonical
cluster route where implemented; the Buddy aliases and missing scope/outbound UI
remain explicitly Unwired. P2-18 remains open. The generated CLI reference is
being exported by hosted run35916213648; no local generation is authorized.

## Windows publication coverage

Windows16 run35914024705 at03456722 is admitted:16 executed,15 passed,1 failed,
0 missing. Exact source bindings, inputs,24 artifact hashes and16 individual
terminals were verified. The native retained-parent rename returned NTSTATUS
0xc0000022 / Win325. This result establishes that specific attempted rename was
refused; it does not prove a universal Windows rule or a successful parent swap.

The replacement selection contains17 explicit cases. One renamed fixture
checks the observed native rename refusal and continued bound publication. A
separate positive case supplies a writable decoy display path while publishing
through the retained real parent, and requires the decoy to remain untouched.
Production publication/rename code is unchanged. Successful physical parent
swapping remains unproven and is not claimed by either replacement identity.

## Evidence boundary and GUI contract

W477 independently reviewed the source repairs. Local verification was bounded
text/diff/JSON/hash work only; no compiler, formatter, parser, test or product ran.
No Slint, visual tokens, labels, layout, focus, controls or accessibility semantics
changed. For design-system/lint_rules.md and AUDIT_CHECKLIST.md, visual/render,
contrast, motion and accessibility checks are N/A to these Rust fixture/Clippy
changes. Implementation integrity was source-reviewed; native GUI runtime and
release validation remain pending rather than receiving a source-only Pass.

P2-19 alone is accepted by the separate38-test W490 receipt. These regression
repairs do not close another functional Road item.
