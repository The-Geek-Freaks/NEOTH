# W224 — Explicit P2-10 registry acceptance selection

A bounded review maps the literal Road requirement to common metadata/readiness
invariants and seven real caller families: direct CLI, Channel, persisted
Background, standalone loop, n8n, Cron and Sub-Agent. W220 covers real persisted
registry A after package B, plus refusal after accepted config drift. W222 adds
actual direct CLI quota fallback after a real later registry publication.

Grouped274 already passed the shared resolver contracts, all11 W197-selected
prerequisite/loader/reload cases, W191 loop, W198 n8n, W199 Cron, W215 Sub-Agent
and W220 Background selections. A direct inspection of its actual fixture names
found four additional existing requirements absent from this targeted lane:

- `pipeline::enriched_request::tests::session_start_skill_registry_is_retained_as_typed_block_d_before_skill_body`
- `cli::serve_pipeline::tests::build_pipeline_handler_retains_selected_generation_through_truthful_retry_before_final_channel_result`
- `cli::serve_pipeline::tests::build_pipeline_handler_retains_context_through_direct_local_shadow_cloud_fallback_final_result`
- `cli::serve_pipeline::tests::authenticated_channel_policy_cannot_inject_a_disabled_subject_skill`

W224 selects these exact existing canonical native identities. Together with
the two W221 retry cases, the grouped lane grows from275 to281. No duplicate
native identity is added and no source test is rewritten in this acceptance
selection. A green older run cannot imply execution of an unselected case.

P2-10 required source-bound terminal results covering every named family
and repair of any demonstrated failure. Grouped275 alone could not close it.
Its literal requirement is registry injection/retention/readiness/isolation;
all-OS release acceptance and real network provider delivery remain independent
requirements. No detached process or provider delivery claim is inferred from
the W220 pre-dispatch fixture.

Local work was limited to text/JSON/Git evidence, with no parser, compiler,
test or runtime execution. The ongoing full native CI on74334d4b is preserved.

## Accepted 2026-09-22

Run `35785415701` on `778637e05d611cba84504adecfcfa325715348b1`
passed all **281/281** exact selected tests. Admission verifies all 58 unique
source-path hashes against Git blobs, selected/executed identities, test-matrix
and Cargo.lock hashes, every actual run section and its one-test pass terminal,
and an empty failed-fixtures file. Both W221 cases and all four W224 cases ran.

The independent review maps the literal requirement to the complete selected
caller set and finds no remaining P2-10 scope gap. P2-10 is therefore accepted;
full native/release acceptance, detached process execution and external provider
delivery retain their own separate gates. Local admission receipt:
`work/gold-20260906/wave224-registry-acceptance/grouped281-778/ADMISSION.json`.