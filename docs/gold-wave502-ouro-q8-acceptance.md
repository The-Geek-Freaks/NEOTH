# W502 Ouro Q8 functional acceptance

GOLD-LF-P1-09 is accepted from seven distinct test/class identities and
13 actual passing platform terminals at bc9db76c4ff72bfff9fa3674f20d904ead12fc76
in GitHub run35871801240: six core/CLI cases on Windows and macOS plus the
macOS-native W151 GUI callback. Root verified both JUnit artifact SHA256s,
each exact classname and test terminal, and the relevant source carry.

The published immutable small Q8 fixture is loaded through the actual artifact
receipt and read lease into QuantizedOuroModel and retained Candle layers.
Two context-sensitive forwards, native/Q8 same-weight comparison and the
per-loop cached/resequenced comparison cover deterministic inference.
Unpublished or malformed artifacts cannot be promoted or reported as native
fallback success. The CLI observer times out while a held worker remains in
flight. The native GUI callback requires typed receipts, clears stale success
on failure, and keeps one in-flight child action.

Nine relevant source blobs, including quantized_layers.rs, are byte-identical
from the tested baseline to42d455ea. The reviewed main.rs carry changes only
W151 test-side Slint string comparisons; the Ouro callback flow is unchanged.
W477 independently reviewed the criterion, carry, hashes and13terminals.

Evidence: [acceptance receipt](verification/gold-wave502-p109-acceptance.json).
The small fixture proves this functional criterion. Production multi-GB model
download/inference, rendered accessibility and full-release readiness are not
claimed. No local executable validation ran. No Slint, design token, layout,
copy or motion changed; visual design-lint and audit dimensions are N/A to
this evidence-only acceptance, and their existing Road items remain open.
