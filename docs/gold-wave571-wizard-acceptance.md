# W571 — real wizard GUI-daemon IPC accepted

GOLD-LF-P1-18 is accepted from its actual criterion terminals. GUI run
35935063658 at829c37c3 executed all148 selected cases, with147 passes and the
separate W480 stream failure. Root admitted three ZIP digests,28Git source/input
bindings, the complete ordered selection/receipts, and every actual terminal.
Both P118 callback cases passed: real bootstrap cancellation cannot replay
Finish, and loss of the real served bootstrap freezes projection and actions.
The13native wizard IPC support cases also passed in Group934 at5a5b13ea.

The scoped files retain identical blobs throughc21e29b1 except main.rs, whose
entire delta is the separately verified W560 hosted-format patch. This carry is
recorded explicitly; no fresh whole-job or identical-HEAD gate is invented for
the two already proven criterion terminals. P2-26a still needs its own corrected
W458/W480 execution, and this acceptance makes no new Slint or cross-OS visual
claim. Road now has1045checked/277open/2partial; WS-LF38done/80open.

- `docs/verification/gold-wave571-p118-acceptance.json`: criterion, scoped carry,
  exact native and GUI terminals.
- `docs/verification/gold-wave571-gui148-terminals.json`: complete GUI admission.
- `docs/verification/gold-wave565-paperless-wizard-terminals.json`: native support.

W569 also imports the exact two-file Rust-format receipt from35938006795 at
c21e29b1 after ZIP/SHA256/before-after Gitblob checks. It changes only Obsidian
CLI/module formatting; no local formatter, compiler or test ran. Permanent
format record: `docs/verification/gold-wave569-format.json`.
