# Vault, reasoning and citation functional acceptance

W451/W452 accept the functional contracts of GOLD-LF-P2-03, GOLD-LF-P1-10
and GOLD-LF-P1-12 against source `5bace4f059f8179eee89cdce3050701107ba3f7f`.
The executions remain tied to their original commits. Reviewed source carry
connects those executions to the accepted source; this is not an assertion
that the current commit passed a new full workspace or release run.

## Nightly vault mirror

FullCI `35871801240` at `bc9db76c4ff72bfff9fa3674f20d904ead12fc76`
provides all 31 required macOS identities and all 29 applicable Windows
identities. These cover policy and credential boundaries, a real bare Git
remote, exact-head verification, receipt-backed retention, uncertain-effect
repair, CLI/GUI/Buddy status, and retained-directory containment. Root checked
the complete individual JUnit terminals and both artifact hashes.

The 17-path dependency map carries 12 paths exactly from the tested commit;
five changed paths have reviewed unrelated or behavior-preserving edits.
The subsequent `b1fd8466` to `5bace4f0` transition changes only formatting in
unrelated BudgetRaft configuration within this dependency set. The macOS
suite's seven cluster SQL failures and Windows suite's unrelated failures
remain recorded. Evidence: `verification/gold-wave451-p203-acceptance.json`.

## Explicit reasoning presentation

The same macOS run provides 17 native reasoning identities, eight universal
GUI reducer identities and both W153 native callback identities. The tests
exercise provider normalization, metadata-only audit, explicit CLI choice,
bounded protocol/runtime state, transient Main/Buddy presentation and history
exclusion. Relevant reasoning route/reducer source carries byte-for-byte;
the GUI entrypoint changes were reviewed and do not change reasoning controls,
callbacks or history behavior. Evidence: `verification/gold-wave452-p110-p112-acceptance.json`.

## Citation lookup and presentation

Citation run `35874789863` at `010d535c84b3966270877ae9bb808b51c8ea35e5`
passed all 50 selected native tests with nine verified source/input bindings.
The native citation sources carry byte-for-byte. The W155 macOS callback in
FullCI `35871801240` proves the presentation route; later GUI boxing,
unused-code cleanup and equivalent cancellation control flow were reviewed.

Crossref and OpenAlex returned live records with independently verified
claim/record bindings. Semantic Scholar returned a typed rate-limit result:
it did not return a live record and establishes no successful live-record
coverage. Its unavailable behavior and the deterministic binding tests meet
the declared functional contract; the Road row does not require simultaneous
successful public-network records from all three providers. Evidence:
`verification/gold-wave433-citation-results.json` and the W452 acceptance JSON.

## Remaining proof boundary

These acceptances close only their three functional Road rows. Packaging,
clean-machine installation, complete affected-platform suites and the frozen
release-candidate gates remain separate. All executable validation took place
on GitHub. No local compiler, formatter, test or product runtime was used.
