# W339 — exact parent acceptance and Group684 follow-up

Group684 run `35838862993` on `b6090b1f71ce4ca2926494b9f77389c56ecdb7d9`
executed all 684 selected cases: 679 passed and five failed. Root verified
149 source-file SHA-256 bindings, the frozen matrix and Cargo lock, every
actual one-test terminal, and the complete ordered selection. The aggregate
lane remains failed; individually proven results remain usable.

Three complete parent requirements now have admitted evidence:

- P2-08: all 51 universal WAL session identities passed, including authenticated
  live egress, legacy zero compatibility and exact-session reader isolation.
- P2-11: all 29 CodeGraph import/type/BFS and current Graphify/self-knowledge
  consumer identities passed, including cycles and stale-generation refusal.
- P2-12: all 25 skill-autonomy cases passed. Both actual GUI callback/receipt
  cases also passed in admitted GUI129 run `35831827281`; their sources remain
  unchanged. The GUI lane's four unrelated failures remain recorded.

The acceptance JSONs bind every required test to the executed Git source.
Their relevant published source blobs are unchanged at `9cd6f9ab`.
This closes these three functional parent rows; it does not close native
platform, GUI-wide, artifact, or release gates. Later source modifications
still require their affected regressions.

W336 repairs the direct-retry structural count and provides the missing real
WAL in its fallback fixture. W337 runs bound interrupted retention recovery
before the full inventory that rejects its temporary paired-namespace state.
W338 corrects quota-backoff call counts and chained role-error assertions.
Independent static review passed for all five remaining failures. Their
current-source behavioral rerun remains pending.

Road: 1324 total = 1025 checked + 297 open + 2 partial; 299 raw and 298 pre-tag
blockers. WS-LF: 18 done / 100 open. No local executable validation ran.
