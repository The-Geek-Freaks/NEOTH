# W518 reflection hygiene acceptance and W519/W520 stream follow-up

GOLD-LF-P2-04 is accepted from the actual criterion-specific evidence:
55 passing exact identities in admitted Group8908003, the same55 exact neoth
JUnit terminals on macOSbc9, and seven fresh Windows storage/retention cases.
The Windows24 run35922329286 at8003a605 passed every selected case. Root
verified all24 ordered identities, source Gitblob bindings and Rust terminals,
32 artifact hashes and four input hashes. This includes three separate Store
publication/share/rename tests supporting the real Windows storage change.

The55 cases cover inclusive90-day retention, bounded resumable batches,
receipt-owned quarantine/purge/restart, canonical integer Jaccard deduplication,
topic synonym cycles and normalization, legacy migration without data loss,
deterministic Daily-only yearly synthesis and exact-once CLI/cron settlement.
Ten relevant source paths were compared from the macOS baseline to8003.
Nine are byte-identical. Only skills/store.rs changed: Windows-only production
code plus focused tests. The fresh Windows cases close that concrete carry gap;
Unix rename and symlink behavior retain actual macOS evidence. W477 approved
the criterion/carry mapping independently. No live multi-year deployment or
whole-release claim follows from these controlled long-horizon fixtures.

Receipt: [P204 functional acceptance](verification/gold-wave518-p204-acceptance.json).

The complete Group8908003 run was888PASS/2FAIL/0missing with176verified
source/input bindings. Its two remaining Chat failures are outside P204.
W519 repairs the producer's terminal wire format: UUID-simple capabilities
were32hex and invalid as GuiChatDigest; lifecycle receipt IDs are now canonical
64hex SHA256 values in Complete, silence-timeout Failed and Indeterminate.
W520 retains the strict CLI reasoning audit and expects redacted/policy_redacted
when the Replace hook intentionally defers/redacts provider output. The former
unsupported expectation described a different state. All content, hash,
receipt, permission/identity, sequencing and recovery assertions stay intact.
W477 reviewed both fixes; actual next-run behavior remains pending.

Preflightcd04 run35923114463 passed formatting. Its packaging contract failed
because the machine-readable Road summary still reflected1040complete instead
of the actual1043. W516 corrects it, then includes this newly accepted P204 row:
1044complete/278open/2partial. The raw backlog and release blocker remain real;
no release-gate check or outstanding checkbox is removed.

No local compiler, formatter, parser, test, model, GUI or runtime was invoked.
No Slint, layout, copy, theme, keyboard/focus or motion was changed. Those visual
review dimensions are N/A for this Rust wire repair and evidence-only closure;
GUI148 at8003 remains separate, and P1-18/P2-26a remain open.
