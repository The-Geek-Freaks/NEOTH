# Named Slack accounts and hosted regression repairs

W664-W667 adds the second named-account runtime after Telegram. Public policy
and both Slack credentials must have exactly matching account IDs; duplicated
YAML keys, mixed legacy fields, missing tokens and invalid fixed sender IDs
reject before adapter admission. Scalar legacy Slack keeps its singleton path.

Each authenticated account receives its own fleet bucket, reload fingerprint,
health binding and inbound authority. Token/policy fingerprint components are
length-framed so different token splits cannot alias. Status exposes account
identity and matching runtime observations without tokens or the operator ID.

Mapped Slack provenance is minted by the runtime bundle and travels through the
production live reply factory into the account/incarnation-bound WAL intent and
result. The ingress adapter ignores claimed human identity and derives sender,
conversation, lease and media scope from its authenticated account. Cross-channel
relabeling fails before transport. Seventeen focused portable tests are selected
in Group1042. Named CLI add/rotate/remove/migrate and proactive Slack routes remain
separate work; the registry makes no new lifecycle support promise in this slice.

W668 admits Windows48 at 009cabbf: 45 pass and three Paperless lifecycle failures.
The diagnostic retained-DELETE contract, released-DELETE twin and source-swap
case pass. The remaining final namespace probe opened a directory incompatible
with the retained DELETE handle. Revalidation now compares the retained stage
identity through the established DELETE-sharing child probe after a fresh
no-follow parent open. Two Windows fixtures cover success while retaining the
handle and refusal of a substituted generation. Windows50 must prove the repair.

W670 addresses the actual Unix socket replacement failure in Group1025. A random
same-directory hard link pins the pre-probe socket inode until recheck/unlink;
otherwise Linux may reuse the inode after an unlink/rebind and make the old
identity comparison accidentally accept a replacement. The daemon PID lock is
required before creating custody, and cleanup removes only its matching custody
leaf. The original replacement race stays intact; a macOS-only real socket test
covers hard-link identity and cleanup on that platform.

W671 admits the partial Group1025 run at 13d13465: 1,015 exact tests completed,
1,014 passed and one socket test failed. The next list attempt failed discovery
because three newly cataloged Cron tests had `tests` instead of
`workstream_c_tests` in their module path. Those identities are corrected without
changing their assertions. Nine later selections did not start. Console output
inside a test line does not erase its exact run identity and final result summary.

W669 closes only GOLD-LF-P2-26a: the real three-chunk producer passes in Group977,
and W480 invokes actual Main/Buddy consumers successfully in GUI149. Block emits
no provider text, completion or CLI preview; Replace exposes only the accepted
body with matching count/hash/terminal identity, preview and Buddy Recents.
All criterion-bearing source carry is checked, including the already admitted
two formatter hunks in GUI main.rs. Packaged macOS/reconnect P2-26b remains open.

W672 admits successful Core35959093253 at 009cabbf. The generated CLI reference is
byte-identical to the committed file (SHA256
8d97c9a7ddd81e8284bdcf724e324084e12a9c3d7aa127056c71f715833ce09f).
The new batch has independent static review; its hosted compilation and behavior
remain pending. The local machine ran no compiler, formatter or executable test.
