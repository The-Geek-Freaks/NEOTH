# W231 — Recall chips and response feedback: separate acceptance contracts

This batch adds two separate selections to the same hosted run. It selects
existing behavior; it introduces no new product path and no new native test
records. The actual Grouped341 union contained neither set. A JSON identity
check confirms every new name exists exactly once in the native catalogue,
and all392 resulting selected names are unique.

## Recall chips — 25 core cases plus 10 separate GUI cases

The P2-27 selection covers the typed EpisodeHit/RecallSourceRef pair, retained
Warm snapshot score, exact provenance, capped content-free projection and
explicit missing/untrusted/incognito outcomes. Positive forged bindings,
negative event sentinels, unknown tiers and importance values cannot become
false Warm evidence. Preload and final dedup retain or remove matching evidence.
Wire frames bind request, control token and sequence; valid Warm provenance is
required for a score.

The GUI116 lane independently selects eight reducer/cursor fixtures and the
W163/W167 registered Main/Buddy callbacks. Terminal, replacement, detach and
cursor fences remain separate requirements. Keyboard/screen-reader behavior
and rendered accessibility are not established by a source selection alone.
P2-27 remains open until all its literal evidence requirements are satisfied.

## Response feedback — 26 core cases plus 5 separate GUI cases

The P2-28 selection covers content-free private storage, opaque response IDs,
exact terminal-session binding, fixed signals, CAS correction/removal,
concurrent requests and review-only evaluation consumption. Incognito creates
no feedback state. CLI mutations require response, session and revision.
Authenticated terminal targets preserve the actual producer target through the
daemon without inventing another ID; writer drain precedes target presentation.

GUI116 separately selects the three response-feedback reducers, the bridge
current-turn fence and W164's actual callback. Actions remain single-flight
and require exact fresh readback before repainting. P2-28 remains open until
its actual core/GUI outcomes and literal contract are verified.

## Verification boundary

Focused selection becomes392; inventory stays535sources/842native/94GUI,
plus22Linux/22macOS extras and26macOS custom cases. Tests remain individually
source-bound even though they share one hosted compilation and dispatch.

This commit also imports the exact Hosted formatter patch from Preflight
35792353017 on c20ed504 for W229 executor/membership. Artifact SHA256SUMS,
source-head, Git preimages, unchanged local preimages, apply-check and exact
postimages were verified. No local formatter, parser, compiler, test or GUI ran.
W229 behavior still requires its actual Hosted result; Road counts are unchanged.

## Full native selection executed

Grouped398 run35795048502 on480ff458 executed every selected case. All25 recall
and all26 response-feedback cases passed, including the two corrected sealed
response module identities. The run as a whole is397 passed/1 failed; its sole
failure is the separate yearly-receipt concurrency case. All83 source paths,
matrix, lock and exact per-test terminals were verified in the retained receipt
at work/gold-20260906/wave234-native-gui/grouped398-480/ADMISSION.json.
GUI116 and literal product acceptance remain independent requirements. These
native passes do not close P2-27/P2-28 or cover subsequent source changes.
