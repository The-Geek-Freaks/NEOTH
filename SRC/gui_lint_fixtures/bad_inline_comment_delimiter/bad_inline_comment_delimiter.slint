export component BadInlineCommentDelimiter inherits Rectangle {
    color: // ; this is a comment, not a statement delimiter
        #ABCDEF;
    font-family: // ; this is a comment, not a statement delimiter
        "Comic Sans MS";
    font-size: // ; this is a comment, not a statement delimiter
        12px;
    border-radius: // ; this is a comment, not a statement delimiter
        8px;
}
