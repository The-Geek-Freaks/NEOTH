// animate width { duration: 120ms; easing: spring; }
// x: animation-tick() * 1px;
/* opacity: animation-tick() / 1s; */
export component Fixture inherits Rectangle {
    text: "animate width { easing: bounce; } x: animation-tick() * 1px; opacity: animation-tick() / 1s;";
}
