# NEOTH GUI — Safe-Gap Hunt Backlog (2026-07-16b, workflow wf_3f7b4708-e0d)

> 5-Agenten read-only Research-Pass (5/5 done, 0 err, 1.1M tokens). 67 findings.
> Ranked: **SHIP-NOW** (pure-slint + non-Codex-hot) first. `[ ]`=offen `[x]`=erledigt.
> Line-Refs = agent-read HEAD; VOR jedem Edit am Code verifizieren (kann driften).


## ✅ SHIP-NOW (pure-slint, non-Codex)

- [ ] **HIGH** `app_shell.slint:374` — StatusFooter in app_shell.slint contains 9 separate Text nodes that each hardcode `font-size: 11px` (lines ~374-413 spanning the status-bar area). Theme.font-size-2xs = 11px exactly — zero visual change, but the literals resist future token-wide rescale and diverge from every other 11px site that already uses the token.
  - fix: Global replace in the StatusFooter block: `font-size: 11px;` → `font-size: Theme.font-size-2xs;` (9 sites, all token-identical).
- [ ] **HIGH** `app_shell.slint:349` — TopBar has no always-visible ⌘K hint chip. Linear and Raycast show a persistent faint pill in the topbar right slot (e.g. '⌘K') that teaches users the palette shortcut exists without them needing to read docs. NEOTH's TopBar (app_shell.slint line 349) exposes a `@children` right slot but the built-in right HorizontalLayout contains nothing except children — no platform discoverability affordance.
  - fix: Expand line 349 from a one-liner to a block with the chip before `@children`:  HorizontalLayout {     alignment: end; spacing: 16px;     // ⌘K hint chip — palette discoverability affordance (Linear grammar)     Rectangle {         height: 22px;         width: cmd-k-lbl.preferred-width + 14px;         border-radius: Theme.radius-pill;         background: Theme.surface-1;         border-width: 1px; 
- [ ] **HIGH** `buddy.slint:352` — Six mood overlays have no entrance fade — they pop onto the orb instantly on mood entry. `thinking` (line 335) and `success` (line 374) already have the `opacity:0; init=>{ self.opacity=1; } animate opacity { duration: Theme.duration-fast; }` pattern. The other six (`working/analyzing` at 352, `alert/consent/problem` at 385, `secure/connected` at 402, `sleeping` at 410, `error/danger` at 417, `notification` at 427) do not. For overlays whose element already owns a continuous-tick opacity (`alert` line 388: `0.75 + 0.25 * root.breathe`, `notification` line 432: `0.70 + 0.30 * root.breathe`, `working` line 357: `0.85 * (1.0 - abs(...))`) a transparent wrapper Rectangle must carry the entrance fade while the inner element retains its own opacity binding. The three Path-only overlays (`secure`, `error`, `sleeping`) can take the attribute directly.
  - fix: For overlays without dynamic own-opacity (secure/connected Path line 402, sleeping Text line 410, error/danger Path line 417): add `opacity: 0; init => { self.opacity = 1; } animate opacity { duration: Theme.duration-fast; easing: ease-out; }` directly to each element.  For overlays where the element itself already owns a breathe/sweep opacity, wrap in an entrance-fade container: ```slint // notif
- [ ] **HIGH** `buddy.slint:471` — The BuddyDock caption chip (`cap`/`lbl`, lines 471-484) hard-swaps text the instant `root.caption` changes. There is no opacity transition on the label. The `changed caption` hook is never used. A Timer-based fade-through (fade label to 0 over 80 ms, swap `shown-caption`, fade back to 1) is the pure-Slint approach — Timer is a built-in element requiring no import.
  - fix: Inside `BuddyDock`, before `bud :=`, add: ```slint property <string> shown-caption: root.caption; property <float> caption-alpha: 1.0; animate caption-alpha { duration: 80ms; easing: ease-in-out; } changed caption => { root.caption-alpha = 0.0; } Timer {     interval: 85ms;     running: root.caption-alpha < 0.05;     triggered => {         root.shown-caption = root.caption;         root.caption-al
- [ ] **HIGH** `buddy.slint:461` — The `TouchArea` inside `bud` in BuddyDock (lines 461-469) has `clicked` and `pointer-event` callbacks but zero accessible attributes. Screen readers cannot identify the orb as a button, and `root.mood` is not announced on state change. The `lbl` Text in the caption chip (line 478) also has no `accessible-role`/`accessible-label`. Both are in BuddyDock scope and can be fixed at the call site without touching the Buddy component definition.
  - fix: In `bud := Buddy { TouchArea { ... } }` (line 461-469), add to the TouchArea: ```slint accessible-role: AccessibleRole.button; accessible-label: "NEOTH buddy — " + root.mood; ```  In `lbl := Text { ... }` (line 478-483), add: ```slint accessible-role: AccessibleRole.text; accessible-label: root.caption; ```
- [ ] **HIGH** `chat.slint:494` — No ConfirmDialog before ChatBubble delete — message destroyed on single click with no confirmation
  - fix: Add `property <bool> del-confirm: false;` to ChatBubble. Change del-ta clicked to set `del-confirm = true`. Mount `if del-confirm: ConfirmDialog { title: "Delete message?"; danger: true; confirmed => { root.delete-clicked(); del-confirm = false; } cancelled => { del-confirm = false; } }`.
- [ ] **HIGH** `chat.slint:184` — Streaming cursor ▋ is a static appended character (chat.slint line 176: `root.message.text + (root.message.streaming ? " ▋" : "")`). Claude desktop and ChatGPT desktop both blink the cursor during streaming to signal 'live output in progress'. NEOTH's cursor is visually indistinguishable from a literal block character in the reply, giving no temporal 'aliveness' cue.
  - fix: Add one property line inside the `body := Text { ... }` block (after `letter-spacing` on line 180, before `color` on line 181):  opacity: root.message.streaming && Theme.animation-mode != 0     ? 0.82 + 0.18 * sin(360deg * (animation-tick() / 700ms))     : 1.0;  This pulses the body text opacity at ~1.4 Hz when streaming, reduced-motion safe (mode==0 returns 1.0), and requires zero new elements. T
- [ ] **HIGH** `components.slint:1289` — CommandPalette has no keyboard-legend footer row. Raycast and Linear both show a persistent '↑↓ navigate · ↵ open · Esc close' bar at the bottom of the modal — the single highest discoverability affordance a palette can have. NEOTH's palette (components.slint line 1184–1290) ends after the Flickable with no footer. New operators who open ⌘K have zero affordance for what keys do what.
  - fix: Insert between the closing `}` of the Flickable (line 1289) and the closing `}` of the card VerticalLayout (line 1290):  Rectangle {     height: 30px;     background: Theme.bg-sunken;     // top hairline separates legend from results     Rectangle { y: 0; height: 1px; background: Theme.border-default; }     HorizontalLayout {         padding-left: 14px; padding-right: 14px;         spacing: 18px; 
- [ ] **HIGH** `components.slint:1274` — CommandPalette selected result row has no '↵' action-key chip. Raycast shows a faint '↵' pill on the right of every selected row so the operator knows what pressing Enter does. NEOTH's result rows (components.slint line 1268–1274) show `item.hint` which is the group-category eyebrow (CORE/WORK/SYSTEM) — a category label, not a keyboard affordance. The operator must guess that Enter activates the selection.
  - fix: Insert after the closing `}` of the `item.hint` Text (line 1274), still inside the HorizontalLayout on line 1251, before the HorizontalLayout closes on line 1275:  if i == root.selected: Rectangle {     height: 18px;     width: enter-hint.preferred-width + 10px;     border-radius: Theme.radius-pill;     background: Theme.accent-tint;     border-width: 1px;     border-color: Theme.border-accent;   
- [ ] **HIGH** `cron.slint:67` — cron.slint lines 67 and 81 use raw string `font-family: "monospace"` for job-id and cron-expr Text elements. Every other mono surface in the codebase uses `Theme.font-mono`. Raw string resolves to system fallback, bypassing the brand JetBrains Mono stack entirely.
  - fix: Replace both occurrences: `font-family: "monospace";` → `font-family: Theme.font-mono;`
- [ ] **HIGH** `cron.slint:128` — No ConfirmDialog before 'Remove' on CronJobCard — destructive action fires immediately on single click
  - fix: Add `property <bool> confirm-remove-open: false;` to CronJobCard. Change Remove button to set that bool; mount `if confirm-remove-open: ConfirmDialog { title: "Remove job?"; body: job.name; danger: true; confirmed => { root.remove-clicked(job.id); confirm-remove-open = false; } cancelled => { confirm-remove-open = false; } }` inside the card.
- [ ] **HIGH** `mesh.slint:611` — No ConfirmDialog before 'Prefer A' / 'Prefer B' conflict-resolution buttons in mesh.slint — permanently discards one peer's data on a single click
  - fix: Add `property <bool> conflict-confirm-open: false; property <string> conflict-pending-side: '';` to the conflict card component. Wire the two resolve buttons to set these, then mount `if conflict-confirm-open: ConfirmDialog { title: "Discard changes?"; body: "Prefer " + conflict-pending-side + " — the other side is lost."; danger: true; confirmed => { root.resolve-conflict(conflict-pending-side); 
- [ ] **HIGH** `selfimprove.slint:92` — No ConfirmDialog on Rollback button in SelfImproveView ProposalCard — rollback fires immediately on single click
  - fix: Add `property <bool> rollback-confirm: false;` to ProposalCard. Change Rollback NeothButton to `clicked => { rollback-confirm = true; }`. Mount `if rollback-confirm: ConfirmDialog { title: "Roll back proposal?"; body: root.title; danger: true; confirmed => { root.rollback-clicked(); rollback-confirm = false; } cancelled => { rollback-confirm = false; } }`.
- [ ] **MED** `app_shell.slint:290` — Operator card badge initials Text at app_shell.slint:290 uses `font-size: 11px` as a raw literal. Theme.font-size-2xs = 11px, token-identical swap.
  - fix: `font-size: 11px;` → `font-size: Theme.font-size-2xs;`
- [ ] **MED** `app_shell.slint:297` — Operator card name Text at app_shell.slint:297 uses `font-size: 13px`. Theme.font-size-sm = 13px, token-identical swap.
  - fix: `font-size: 13px;` → `font-size: Theme.font-size-sm;`
- [ ] **MED** `app_shell.slint:174` — NEOTH wordmark Text at app_shell.slint:174 hardcodes `letter-spacing: 1px`. Theme.letter-spacing-tight = 1px — token-identical, but the raw literal breaks theme-wide letter-spacing control.
  - fix: `letter-spacing: 1px;` → `letter-spacing: Theme.letter-spacing-tight;`
- [ ] **MED** `buddy.slint:326` — The face mouth at line 326-328 has exactly two SVG commands: smile (`M44 55 Q50 62 56 55`) for happy/success/excited, and flat neutral (`M45 57 L55 57`) for ALL other moods including alert, error, danger, problem — where a subtle frown would communicate distress and make the companion feel alive. The face block at line 303 already excludes sleeping and error from showing the face at all for `error`, so only `alert`/`problem`/`danger` visibly benefit.
  - fix: Replace lines 326-328 with a three-branch expression: ```slint commands: root.is-mood("happy") || root.is-mood("success") || root.is-mood("excited")     ? "M44 55 Q50 62 56 55"     : root.is-mood("alert") || root.is-mood("problem")           || root.is-mood("consent") || root.is-mood("danger")         ? "M44 58 Q50 53 56 58"         : "M45 57 L55 57"; ``` The frown command `M44 58 Q50 53 56 58` mi
- [ ] **MED** `buddy.slint:474` — The BuddyDock caption chip width at line 474 is `lbl.width + 16px` with no upper bound. `lbl` has no `width` constraint, so `lbl.width` equals the full rendered text width. If Rust writes a long caption such as `"connecting to provider…"` or `"waiting for consent gate"`, the chip expands well beyond the 96px container, overflows off-screen to the left (cap.x = (96 - chip_width)/2 goes negative), and may cover the main window content. No `overflow: elide` is set on the label.
  - fix: Replace lines 473-483 with: ```slint height: root.caption != "" ? lbl.height + 6px : 0px; width: root.caption != "" ? min(lbl.preferred-width + 16px, 200px) : 0px; border-radius: Theme.radius-pill; background: Theme.surface-glass; border-width: 1px; border-color: Theme.border-subtle; lbl := Text {     x: 8px; y: 3px;     width: parent.width - 16px;   // filled by parent, not content-driven     tex
- [ ] **MED** `buddy.slint:306` — Both eye rectangles at buddy.slint lines 306 and 314 have hardcoded viewbox x-positions (42.4u and 54.2u). There is no lateral gaze offset for any mood. Best-in-class companions shift eyes toward the 'scanning' direction for searching/analyzing — a subtle horizontal oscillation that reads as active attention and differentiates from the idle blink.
  - fix: Add a gaze property to the Buddy root (after the `idle-blink-scale` block, e.g. line 131): ```slint property <float> gaze-dx: (root.is-mood("searching") || root.is-mood("analyzing"))     ? (Theme.animation-mode == 0 ? 0.0         : sin(360deg * mod(animation-tick(), 1800ms) / 1800ms) * 1.8)     : 0.0; ``` Then update eye x-positions: ```slint // left eye (line 306): x: (42.4 + root.gaze-dx) * root
- [ ] **MED** `buddy.slint:437` — BuddyDock has no accessible-role or accessible-label — screen readers cannot announce the persistent AI companion widget
  - fix: Add `accessible-role: button; accessible-label: "NEOTH buddy — " + root.mood + " — click to open menu"; accessible-action-default => { root.clicked(); }` to the BuddyDock root Rectangle.
- [ ] **MED** `chat.slint:703` — Chat composer '📎' attach button is emoji-only with no TooltipArea — label describes nothing
  - fix: Wrap the `NeothButton { label: "📎"; }` in `TooltipArea { text: "Attach file"; }`. NeothButton already has FocusScope so no ring change needed.
- [ ] **MED** `chat.slint:1228` — Consent strip Gated / Full-Auto mode-toggle buttons have no TooltipArea — operator cannot discover what each mode implies without clicking
  - fix: Wrap each consent-mode NeothButton in `TooltipArea { text: "Gated: every tool call requires approval" | "Full-Auto: all tool calls approved automatically"; }`.
- [ ] **MED** `chat.slint:1007` — Chat session sidebar rows use `overflow: elide` on truncated session titles but have no TooltipArea — full title irrecoverable on hover
  - fix: Wrap each session row Rectangle in `TooltipArea { text: session.title; }` so the full unelided title appears on hover.
- [ ] **MED** `chat.slint:1392` — Scroll-to-bottom FAB mounts with no entrance animation. The FAB (chat.slint line 1384 `if !root.auto-follow: Rectangle { ... }`) is conditionally mounted — Slint fires `init` on mount, so an opacity fade-in is trivially achievable. Every other transient element in the codebase (Toast, CommandPalette card, ContextMenu card, bubble action bar) uses a fade. The FAB snapping in abruptly breaks the 'aus einem Guss' motion contract.
  - fix: Add three lines inside the FAB Rectangle block, after `border-color: Theme.border-accent;` (line 1392):  opacity: 0; init => { self.opacity = 1; } animate opacity { duration: Theme.duration-fast; easing: cubic-bezier(0.16, 1, 0.3, 1); }
- [ ] **MED** `chat.slint:1501` — Link-chips above the composer have no hover preview showing the chip target. Obsidian shows a floating preview card when hovering over any link. NEOTH's link-chip for-loop (chat.slint line 1478) renders only `chip.label` text with a border-color animate on hover — the operator cannot know the target `chip.kind` or `chip.id` without clicking. The TooltipArea component already implements the delayed-reveal pattern but is not used on link-chips.
  - fix: Inside the chip Rectangle body (after link-chip-ta closes on line 1501, before the Rectangle closes on line 1502), add an inline tooltip using the same pattern as TooltipArea:  chip-tt := Rectangle {     x: 0;     y: -self.height - 4px;     height: 20px;     width: chip-tt-txt.preferred-width + 12px;     background: Theme.surface-glass;     border-radius: Theme.radius-sm;     border-width: 1px;   
- [ ] **MED** `components.slint:1610` — MenuItem struct has no shortcut field. Linear and Raycast context-menus always render a right-aligned faint mono shortcut chip (e.g. '⌘D' for delete, '⌘C' for copy) on every menu row. NEOTH's MenuItem struct (components.slint line 1607–1611) has `label`, `glyph`, and `danger` but no `shortcut` field. The ContextMenu row HorizontalLayout (line 1671–1706) has nowhere to render the hint, so no menu in the app can ever show keyboard shortcuts.
  - fix: 1. Add to MenuItem struct (line 1610, before closing `}`): shortcut: string,   // e.g. "⌘D" — empty string hides the chip  2. In ContextMenu's row HorizontalLayout (after the label Text, ~line 1695 before the TouchArea): if item.shortcut != "": Text {     text: item.shortcut;     font-family: Theme.font-mono;     font-size: Theme.font-size-2xs;     color: item.danger ? Theme.consent.with-alpha(0.5
- [ ] **MED** `overlay.slint:231` — In the compact overlay strip, the drag `TouchArea` (lines 214-226) covers the full left 64px × 44px zone at `x:0 y:0`. The `orb-cell` Rectangle (line 231) is visually on top (declared later in the sibling list), but it contains no `TouchArea` of its own. Pointer events on the orb area therefore fall through to the drag handler and fire `drag-started()` on tap. No click-to-expand shortcut exists in compact mode.
  - fix: Inside `orb-cell := Rectangle { ... }` (line 231), after the badge `if` block, add: ```slint TouchArea {     width: 44px; height: 44px;     mouse-cursor: default;     accessible-role: AccessibleRole.button;     accessible-label: "expand NEOTH companion";     clicked => {         root.compact = false;         root.compact-toggled(false);     } } ``` Because `orb-cell` is rendered above the drag `To
- [ ] **MED** `overlay.slint:178` — The bubble dismiss `TouchArea` at overlay.slint line 178-180 has a single `clicked` callback and no `accessible-role`, `accessible-label`, or `mouse-cursor`. Screen readers cannot discover the dismiss action; the cursor stays default (not `pointer`) giving no affordance hint.
  - fix: Replace lines 178-180: ```slint TouchArea {     accessible-role: AccessibleRole.button;     accessible-label: "dismiss buddy message";     mouse-cursor: pointer;     clicked => { root.bubble-dismissed(); } } ```
- [ ] **MED** `overlay.slint:355` — Mic glyph button in MiniOverlay compact strip ('◉') has no TooltipArea and no accessible-role — icon-only button undiscoverable by keyboard and screen readers
  - fix: Add `accessible-role: button; accessible-label: "start voice input"; accessible-action-default => { root.mic-clicked(); }` to the mic-btn Rectangle. Wrap Text+TouchArea in `TooltipArea { text: "Start voice input"; }`.
- [ ] **MED** `overlay.slint:491` — Overlay icon-only buttons compact-toggle '▸' (line 491), restore '⤢' (line 516), close '✕' (line 539) have no TooltipArea — purpose undiscoverable on hover
  - fix: Wrap each button's Text+TouchArea in `TooltipArea { text: "Collapse to pill" | "Restore main window" | "Hide overlay"; }`. Add matching `accessible-label` to the outer Rectangle.
- [ ] **MED** `overview.slint:392` — COST & USAGE chevron in OverviewView is an interactive Rectangle with no FocusScope — not reachable or activatable by keyboard
  - fix: Add `cvu-focus := FocusScope { key-pressed(ev) => { if ev.text == "\n" { cvu-ta.clicked(); return accept; } return reject; } }` and wire `border-color: cvu-focus.has-focus ? Theme.focus-ring : ...` on the chevron Rectangle.
- [ ] **MED** `selfimprove.slint:76` — Accept button in ProposalCard (selfimprove.slint) is a bare Rectangle+TouchArea with no accessible-role or accessible-label — keyboard users cannot trigger this consent action
  - fix: Add `accessible-role: button; accessible-label: "accept proposal " + root.title; accessible-action-default => { root.accept-clicked(); }` to the Accept Rectangle.
- [ ] **MED** `selfreprog.slint:210` — Accept button in SelfReprogRow is a bare Rectangle+TouchArea with no accessible-role or accessible-label
  - fix: Add `accessible-role: button; accessible-label: "accept proposal " + root.row-id; accessible-action-default => { root.accept-clicked(); }` to the Accept Rectangle.
- [ ] **MED** `tabs.slint:120` — ProbeView run button (and all 4 DoctorView sub-probe buttons) are bare `TouchArea { clicked => ... }` with no `accessible-role` or `accessible-label`. Screen readers cannot identify or activate them. NeothButton at components.slint:36 sets `accessible-role: button; accessible-label: root.label` — these buttons follow no such pattern.
  - fix: Replace `TouchArea { clicked => { if (!root.running) { root.run-clicked(); } } }` with: `TouchArea { accessible-role: button; accessible-label: root.running ? "busy" : root.action-label; clicked => { if (!root.running) { root.run-clicked(); } } }` — apply the same pattern to all 4 DoctorView sub-probe TouchAreas at lines 192, 233, 274, 315 with contextual labels ("Run neoth doctor", "Run neoth sta
- [ ] **LOW** `activity.slint:157` — ActivityRow body HorizontalLayout at activity.slint:157 sets `padding: 9px` — off-grid by 1px (space-2=8px). Row height is computed from `preferred-height + 18px` so the misalignment compounds to a 2px error in card height.
  - fix: `padding: 9px;` → `padding: Theme.space-2;`  (8px)
- [ ] **LOW** `activity.slint:149` — activity.slint SideIn row items have no accessible-role: list-item or accessible-label — screen readers cannot enumerate the live background-work feed
  - fix: On each `SideIn { ... }` in the for-loop add `accessible-role: list-item; accessible-label: r.kind + " — " + r.title + " " + r.ts;`.
- [ ] **LOW** `app_shell.slint:69` — NavBtn HorizontalLayout at app_shell.slint:69 sets `spacing: 11px` — off the 4px grid by 1px (nearest tokens: space-2=8px, space-3=12px). Causes icon-to-label gap that differs from all other HorizontalLayout rows in the shell which use space-3.
  - fix: `spacing: 11px;` → `spacing: Theme.space-3;`  (12px, 1px delta, tightens to grid)
- [ ] **LOW** `app_shell.slint:19` — NavGroup VerticalLayout at app_shell.slint:19 sets `padding-bottom: 5px` — off-grid (nearest: space-1=4px). All other NavGroup padding uses 4px/8px.
  - fix: `padding-bottom: 5px;` → `padding-bottom: Theme.space-1;`  (4px)
- [ ] **LOW** `buddy.slint:410` — The `sleeping` mood overlay at line 410-413 is a floating `Text` element with no `accessible-role` or `accessible-label`. Similarly the `error/danger` Path (line 417) and `secure/connected` Path (line 402) have no accessible attributes. Screen readers observing these overlays get no semantic hint about current companion state.
  - fix: Add to the sleeping Text (line 410-413): ```slint accessible-role: AccessibleRole.text; accessible-label: "buddy is sleeping"; ``` Add to the error/danger Path (line 417): ```slint accessible-role: AccessibleRole.text; accessible-label: "error"; ``` Add to the secure/connected Path (line 402): ```slint accessible-role: AccessibleRole.text; accessible-label: "secure connection"; ```
- [ ] **LOW** `components.slint:1252` — CommandPalette result-row HorizontalLayout at components.slint:1252 uses `spacing: 11px` — off the 4px grid. The palette is a canonical component used everywhere; the gap diverges from the 12px that the Expander and nav rows target.
  - fix: `spacing: 11px;` → `spacing: Theme.space-3;`  (12px)
- [ ] **LOW** `n8n.slint:151` — n8n.slint, dreaming.slint, selfimprove.slint, selfreprog.slint, and calendar.slint all use bare Rectangle+Text for zero-data states instead of the EmptyState component
  - fix: In each panel replace the inline bare-text block with `EmptyState { glyph: "○"; title: "<panel title>"; hint: "<panel hint>"; }`. Affects n8n.slint:151, dreaming.slint:251, selfimprove.slint:288, selfreprog.slint:351, calendar.slint:134.
- [ ] **LOW** `overview.slint:365` — overview.slint AGENTS card shows bare Text 'No agents running.' instead of EmptyState component
  - fix: Replace the `Text { text: "No agents running."; }` block with `EmptyState { glyph: "⚇"; title: "no agents running"; }` sized to the card height.

## ⚠ pure-slint · CODEX-HOT

- [ ] **HIGH** `main.slint:3578` — DoctorView has no `init =>` auto-load — operator must manually click all 4 probe buttons on first open, unlike every ProbeView panel (mcp/hooks/groundtruth/catalog/quota/tweaks all auto-run via `init =>`).
  - fix: Inside the DoctorView block at line 3578 (after undo-run-clicked), add: `init => { if (root.doctor-output == "") { root.doctor-run-clicked(); } }`  All 4 Rust callbacks (on_doctor_run_clicked, on_doctor_status_run_clicked, on_doctor_security_run_clicked, on_doctor_undo_run_clicked) are already wired at main.rs:3482/3520/3556/3596 — no new Rust needed.
- [ ] **HIGH** `main.slint:3549` — `ov-cost-open-clicked` callback routes `root.nav-active = "overview"` — clicking the "→ open cost & usage detail" chevron on the Overview cost card keeps the user on the same panel (no-op navigation). The callback fires but produces no visible change.
  - fix: Change `ov-cost-open-clicked => { root.nav-active = "overview"; }` to `ov-cost-open-clicked => { root.nav-active = "config"; }` so the chevron navigates to the Config/Presets panel that contains the full cost session breakdown. No Rust change needed.
- [ ] **MED** `settings.slint:184` — TabButton in SettingsView has no FocusScope — keyboard Tab cannot navigate settings tabs, violating focus-visible contract
  - fix: Add `tab-focus := FocusScope { key-pressed(ev) => { if ev.text == "\n" || ev.text == " " { root.clicked(); return accept; } return reject; } }` inside TabButton. Set `border-color: tab-focus.has-focus ? Theme.focus-ring : (root.active ? Theme.accent : Theme.border-subtle);` on the root Rectangle.
- [ ] **LOW** `agents_view.slint:392` — agents_view.slint empty state for zero-agent list uses raw VerticalLayout+Text instead of the shared EmptyState component — inconsistent with other panels
  - fix: Replace the custom VerticalLayout+Text block with `EmptyState { glyph: "⚇"; title: "no agents running"; hint: "start the daemon or dispatch an agent from the chat"; }`.

## ⚠ needs-live-verify

- [ ] **HIGH** `buddy.slint:458` — The `TouchArea` for the BuddyDock orb is nested inside `bud := Buddy { ... }` as a `@children` child (line 461). Its `has-hover` and `pressed` properties are unreachable from BuddyDock scope (Slint does not allow deep dot-path access into a named component's anonymous children). The Buddy component has no `in property <bool> hovered` or `in property <bool> pressed`, so there is zero visual response to hover or press on the orb — it feels dead compared to Claude Desktop's orb or any Dynamic Island element.
  - fix: Two-step restructure:  1. Add to `Buddy` component (pure-slint — Buddy.slint): ```slint in property <bool> hovered: false; in property <bool> pressed: false; // Drive glow boost: property <float> glow-f: (root.intensity() + (root.hovered ? 0.18 : 0.0) - (root.pressed ? 0.05 : 0.0)) * (0.7 + 0.3 * root.pulse); ```  2. In BuddyDock, move the TouchArea OUT of `bud` and make it a sibling overlay (need
- [ ] **HIGH** `chat.slint:131` — Copy action in chat hover-bar has no momentary 'Copied ✓' feedback. Claude desktop shows 'Copied ✓' for ~1.5s then reverts to '⧉'. NEOTH's copy chip (chat.slint line 441–455) fires `copy-clicked()` and shows no visual acknowledgment at all — the operator gets zero confirmation the clipboard write happened. Same gap applies to the code-panel Copy chip.
  - fix: 1. Add to ChatBubble component scope (after `private property <bool> code-collapsed` on line 131): private property <bool> _copy-done: false; copy-reset := Timer { interval: 1500ms; running: root._copy-done; triggered => { root._copy-done = false; } }  2. Change the copy-chip Text (line 441) to: text: root._copy-done ? "✓" : "⧉"; color: root._copy-done ? Theme.accent-green     : (copy-ta.has-hover

## ⚠ needs-live-verify · CODEX-HOT

- [ ] **HIGH** `settings.slint:793` — Settings channel-remove-clicked, skill-uninstall-clicked, plugin-remove-clicked are destructive but the tab bodies wire no ConfirmDialog before firing them
  - fix: For each list-row Remove/Uninstall button add a local `property <bool> confirm-open` and `property <string> pending-id`. Wire the button to set these; mount `if confirm-open: ConfirmDialog { title: "Remove?"; danger: true; confirmed => { root.channel-remove-clicked(pending-id); confirm-open = false; } }` in the tab VerticalBox. Same pattern for skill-uninstall and plugin-remove.

## ⚠ needs-live-verify

- [ ] **MED** `activity.slint:105` — Activity sidecar row-count chip Text at activity.slint:105 uses `font-size: 10px`. Below token floor (2xs=11px). Inconsistent with the ACTIVITY kicker label directly above it which correctly uses `Theme.font-size-2xs`. Visual change: +1px on the badge count numeral.
  - fix: `font-size: 10px;` → `font-size: Theme.font-size-2xs;`
- [ ] **MED** `app_shell.slint:341` — TopBar kicker Text at app_shell.slint:341 uses raw `font-size: 10px`. No token exists at 10px; the floor is font-size-2xs=11px. Every other kicker/eyebrow label in the app already uses font-size-2xs. Visual change: +1px on the view-name label.
  - fix: `font-size: 10px;` → `font-size: Theme.font-size-2xs;`
- [ ] **MED** `app_shell.slint:96` — NavBtn count-chip Text (app_shell.slint:96) and ComplexitySeg segment labels (app_shell.slint:125) both use `font-size: 10px`. Below token floor. These are the most-visible sidebar elements; both should align to font-size-2xs like every other sidebar micro-label. Visual change: +1px in the nav rail.
  - fix: `font-size: 10px;` → `font-size: Theme.font-size-2xs;` at lines 96 and 125
- [ ] **MED** `app_shell.slint:15` — NavGroup sidebar section headers are purely decorative — clicking them does nothing. Obsidian's sidebar collapses/expands sections by clicking the group label. NEOTH's NavGroup component (app_shell.slint line 15–28) is a static 28px Text row with no state, no TouchArea, no collapse logic. The NavBtns that follow it in the Sidebar Flickable are flat siblings, not children, so a section collapse requires restructuring the flat list into grouped sub-layouts with animated clip heights.
  - fix: 1. Add to NavGroup: `in-out property <bool> collapsed: false;` and wrap the header Text in a Rectangle with TouchArea that toggles `root.collapsed`. Add a chevron Text ('▾'/'▸') beside the label.  2. In Sidebar's Flickable VerticalLayout: replace the flat sequence    `NavGroup { label: "WORK"; }`    `NavBtn { ... } NavBtn { ... }` with a grouped sub-layout:    NavGroup { label: "WORK"; }  // now w
- [ ] **MED** `mesh.slint:147` — PeerCard in mesh.slint has six sub-label Text elements (role chip at line 147, version at 160, last-seen at 178; CPU/RAM/VRAM meter labels at 200/220/240) all set to `font-size: 9px`. No token at 9px; font-size-2xs=11px is the floor. Visual change: +2px bump on dense peer-card text — needs visual confirm the layout holds.
  - fix: Replace all six occurrences of `font-size: 9px;` in PeerCard with `font-size: Theme.font-size-2xs;`
- [ ] **LOW** `activity.slint:126` — Activity sidecar collapse chevron Text at activity.slint:126 uses raw `font-size: 16px`. No token at 16px (font-size-sm=13, font-size-base=15, font-size-md=17). The glyph-size for an interactive chevron should match the token-ladder or use a fixed icon-size token. Visual difference from either neighbour token is 1-2px.
  - fix: Pick `Theme.font-size-md` (17px, +1px) to size the glyph, or add `icon-size-md: 16px` to theme.slint and reference it. Verify chevron does not clip inside its 28x28 Rectangle at 17px.
- [ ] **LOW** `app_shell.slint:345` — TopBar title Text at app_shell.slint:345 uses raw `font-size: 26px`. No theme token at 26px (font-size-xl=24px, next defined display size is 32px). The title is the most prominent heading in the shell; a token-less literal breaks the type scale.
  - fix: Either add `font-size-2xl: 26px;` to theme.slint and use it here, or round to `Theme.font-size-xl` (24px, -2px). Design call required before committing.

## ⚠ needs-rust

- [ ] **HIGH** `cron.slint:139` — `neoth todo list --output json` surfaces external task backends (Todoist, CalDAV, MS-Todo) with full JSON output — confirmed in todo.rs with OutputFormat::Json. The Coding panel has the Kanban for internal `neoth kanban` tasks, but external todo service tasks have zero GUI surface. High daily-driver value.
  - fix: Add a `TodoRow` struct and a `TodoView` subsection (or a second scrollable list) to CronView below the scheduled-jobs list. Wire a `todo-refresh` callback in main.rs that shells `neoth todo list --output json`, parses the array, and pushes rows. A ProbeView (text-dump style) is acceptable as V1 since the JSON is flat. Minimal nav change: add a "Tasks" subheader within the Automation panel rather t

## ⚠ needs-rust · CODEX-HOT

- [ ] **HIGH** `overview.slint:65` — `neoth goal show --output json` has full JSON output (confirmed in goal.rs:98–108 with `OutputFormat::Json`) but is entirely absent from the GUI. The active goal text and grind objective are operator-set mission objectives with no overview card, no property, and no panel surface. overview.slint lists STATUS/TOKENS/AGENTS/HEMISPHERES/SKILLS/CALENDAR/CONSENT cards — GOAL is missing.
  - fix: Add to the OverviewView property block (after line 71, within STATUS card group): `// GOAL card\nin property <string> ov-goal: "";\nin property <string> ov-grind: "";\ncallback ov-goal-edit-clicked();` Then insert a GOAL card in the left column between STATUS and TOKENS. Rust side: in overview refresh, call `neoth goal show --output json`, parse {"goal": ..., "grind": ...} and set ov-goal / ov-gri

## ⚠ needs-rust

- [ ] **MED** `cron.slint:157` — `neoth jobs --bg --output json` lists detached background processes with status/exit code (confirmed in jobs.rs:410–424). CronView (Automation panel, cron.slint:139) only covers `neoth cron` scheduled jobs. Background ad-hoc jobs spawned via `neoth jobs --run "..."` are invisible in the GUI.
  - fix: Add `in property <string> bg-jobs-output: ""; in property <bool> bg-jobs-running: false; callback bg-jobs-refresh();` to CronView and render a ProbeView-style section below the cron list titled "Background Jobs". Wire `bg-jobs-refresh` in main.rs to shell `neoth jobs --bg --output json`. cron.slint is in the solo lane — only the main.rs callback is codex-hot.

## ⚠ needs-rust · CODEX-HOT

- [ ] **MED** `main.slint:3601` — `neoth slash list --output json` outputs all loaded slash commands with names and descriptions (confirmed in slash.rs:78–90). The only GUI surface is a `/` green badge in the chat composer (chat.slint:716) that indicates slash mode is active — there is no browseable command list anywhere. Operators cannot discover what slash commands exist without dropping to the CLI.
  - fix: Add a second ProbeView section to the Hooks panel (currently at line 3601) for `neoth slash list --output json`, or add a dedicated nav entry "Slash" (complexity >= 1) in app_shell.slint between Hooks and Catalog. Wire `slash-output` / `slash-running` / `slash-run-clicked` in main.rs identical to the hooks probe at main.rs:3720.

## ⚠ needs-rust

- [ ] **MED** `mesh.slint:808` — REDUNDANCY Led in mesh.slint is hardcoded to state: 'connecting' regardless of actual cluster redundancy — misleads operator
  - fix: Replace with `Led { state: root.redundancy-state; }` driven by a new `in property <string> redundancy-state: "off";` and wrap in `TooltipArea { text: "Redundancy: " + root.redundancy-state; }`. Rust derives state from peer count.
- [ ] **MED** `overview.slint:1` — OverviewView has no loading/skeleton state — cards show '—' fallback permanently until Rust pushes data, no visual distinction between loading and unavailable
  - fix: Add `in property <bool> overview-loading: false;` to OverviewView. In each card body: `if root.overview-loading: Skeleton { rows: 3; }` before the data content. Rust sets it true before the refresh probe and false on completion.

## ⚠ needs-rust · CODEX-HOT

- [ ] **MED** `settings.slint:1835` — `neoth mode list --output json` lists all loaded behavior modes with trigger phrases and active model (mode.rs:31–45, JSON confirmed). Skills tab in settings shows raw skills but not the derived mode registry — operators cannot see which modes would activate for a given message or verify their mode setup.
  - fix: Add a ProbeView-style section at the bottom of the Skills tab (SettingsTab.skills, currently ends around line 2070) titled "Mode Registry" with a Run button that calls `neoth mode list --output json`. Wire `mode-list-output` / `mode-list-running` / `mode-list-run-clicked` in main.rs. Alternatively a top-level ProbeView nav entry alongside catalog/quota/tweaks.
- [ ] **MED** `settings.slint:2256` — `neoth profile show --output json` outputs extracted identity and preference claims (confirmed profile.rs:909–912 with OutputFormat::Json). The Memory panel in settings shows raw WAL/SQLite memory rows but not the synthesized user profile (identity, communication style, behavioral knobs). Operators cannot review or redact profile claims from the GUI.
  - fix: Add a "User Profile" Expander section to SettingsTab.memory (currently ends around line 2555) with a ProbeView-style output pane backed by `neoth profile show --output json`. Wire `profile-output` / `profile-running` / `profile-run-clicked` in main.rs. A "Redact field" button invoking `neoth profile redact <field>` would be the write-back action (needs-rust, Elevated autonomy gate).

## ⚠ needs-rust

- [ ] **MED** `tabs.slint:148` — `neoth monitor --json` outputs request counts, panic/crash counts, and channel health over a configurable lookback window (monitor.rs:224–237). This proactive health view has no GUI surface — not in DoctorView, not in Overview, not in Resources.
  - fix: Add a 5th section to DoctorView: `in property <string> monitor-output: ""; in property <bool> monitor-running: false; callback monitor-run-clicked();` with a "Monitor" HorizontalLayout header (label "Proactive monitor — request counts, crash events, channel health (neoth monitor --json).") and a scrollable Rectangle output pane. Wire in main.rs identical to the security-audit section at main.rs:35

## ⚠ needs-rust · CODEX-HOT

- [ ] **LOW** `main.slint:3612` — `neoth adr list --output json` outputs architecture decision records (adr.rs:51–62). No GUI panel exists. ADRs are operator-created decision artifacts that inform the model — invisible in the GUI means operators can't review what decisions are active without CLI.
  - fix: Add a second ProbeView to the Groundtruth panel (currently at line 3612) titled "Architecture Decisions" with blurb "Decision records — operator-ratified architectural choices fed into the system prompt (neoth adr list)." Wire `adr-output` / `adr-running` / `adr-run-clicked` in main.rs to call `neoth adr list --output json`.
- [ ] **LOW** `main.slint:3632` — `neoth search --stats --output json` outputs web search usage analytics: top queries, success/fail/cache-hit counters (search.rs:99–106 with OutputFormat::Json). No GUI surface exists — operators cannot inspect their web-search usage patterns from the GUI.
  - fix: Add a ProbeView section to the Quota panel (currently at line 3632) or a dedicated sub-section titled "Web Search Stats" with blurb matching search.rs semantics. Wire `search-stats-output` / `search-stats-running` / `search-stats-run-clicked` in main.rs to call `neoth search --stats --output json`.
## ⚠ VERIFY-FIRST CORRECTION (2026-07-16b, Claude)
- **ConfirmDialog findings [chat delete / cron remove / mesh conflict / selfimprove rollback] RECLASSIFIED needs-live-verify.** ConfirmDialog (components.slint:1379) is a full-scrim modal centering on `root.width`, has ZERO existing mounts, and the finding's in-card mount would scope the scrim to the card. Correct impl = view-root mount + z-order-last + card→view callback plumbing, whose visual correctness (scrim coverage, centering, z-order) is not locally verifiable under the BSOD no-live-GUI rule. Deferred to a live-verify session.
- **TooltipArea-wrap findings** similarly deferred: hover-popup positioning is not locally verifiable. Pure `accessible-role`/`accessible-label` additions (zero visual, compile≈correct) ARE being shipped.
