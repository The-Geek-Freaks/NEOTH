# BUILD LOG — 2026-07-17 (build-all-then-hunt strategy)

Strategy: build every loop-buildable open ROAD item + emergent sub-items FIRST,
recording exact wiring (file:line, what/how) here, THEN one consolidated
test + bug-hunt pass using this log for fast localization.

Gate policy during build phase: `cargo check -p neoth --tests` (compile only)
after each wave. Focused tests + bug-hunt run once the build phase closes.

Scope excluded (multi-session release epics): GOLD-R3-10, R4-01/02/04/07/08/09/10/12/13.

---

## Wave D1 — daemon code items

### BUG-W2-P1-HOOK-CHANNEL-PARITY / HOOK-ONCE-PARITY
- root cause: `once` never read by dispatcher; each caller does TOCTOU check-then-insert around a synchronous set → concurrent channel turns / repeated cron ticks both fire. serve_pipeline PreProviderCall discards FilteredBlocks (no restore path).
- built (hooks/, agent): `SessionOnceGuard(Arc<Mutex<HashSet>>)` dispatcher.rs:529 (Clone=Arc-share); `StageOnceResult{outcome, filtered_blocks, skipped_once}` :562; `run_stage_with_once_guard(stage, body, hooks, invoker, fail_fast, &guard) -> Result<StageOnceResult>` :609 (atomic claim-before-effect under mutex, lock never across await); `restore_blocks(body, &[FilteredBlock])` block_filter.rs:130. Re-exported hooks/mod.rs:20-22. Old fns unchanged. Tests: once_guard_concurrent_claims_allow_exactly_one_fire / once_guard_sequential_second_call_is_skipped / post_provider_call_with_once_guard_and_block_filter_restoration.
- CALLER WIRING (delegated to wiring agent; owns chat.rs/serve_pipeline.rs/cron/runner.rs):
  - chat.rs ~5040 HashSet→SessionOnceGuard; run_hook_stage param; delete pre-filter ~5403-5437 + post-insert ~5470; call run_stage_with_once_guard + emit HOOK_SKIPPED_ONCE per skipped_once name; 5 sites (2246,2296,3720,5075,5232) pass &once_guard.
  - serve_pipeline.rs 812 Arc<Mutex<HashSet>>→Arc<SessionOnceGuard>; PreChannelIngress ~1047 / PreEgress ~620 / PreProviderCall ~2320 → run_stage_with_once_guard, KEEP result.filtered_blocks as pending_blocks; ADD PostProviderCall after LLM reply: restore_blocks(&reply,&pending_blocks)+run stage.
  - cron/runner.rs 7 sites (179,1807,1851,1910,1964,2005,2043): per-job SessionOnceGuard, run_stage fn-ptr → closure mapping StageOnceResult→outcome.

### FOLLOW-UP-W3-TOKENCAP-DAEMON-DEGRADE — BUILT (agent, self-contained)
- `finalize_daemon_request(prompt, Option<system>, cap) -> Result<DaemonBudgetOut{system,prompt,total_tokens}, &str>` tokens/budget.rs ([A(system?),E(prompt)], enforce_budget_to_fit, fail-close if A/E can't fit). Tests: fits_unchanged / over_cap_e_only_with_a_protected_returns_err / a_and_e_cannot_fit_returns_err.
- routed: serve_tasks.rs:2046 (OMI; cap=reload_controller.latest().tokens.max_per_request), n8n_api/handlers.rs:370 (cap=config.tokens.max_per_request), session_sort_cron.rs:176 (+cap param thru group_titles/run_session_sort_pass; spawn loop 570-585 effective_cap). Caps = existing reload/config path.

### FOLLOW-UP-W3-SMARTAPPROVE-DUP-TOOL-CLIENT — BUILT (agent, self-contained)
- smart_approve.rs initialize_server ~404: `has_grants=!verdicts.is_empty()`; seed_verdicts unconditional (seal stays); insert_live only if has_grants; else client drops → McpClient::Drop → child.start_kill() (kill_on_drop client.rs:243). Test: duplicate_tool_seal_no_client_retained_and_confirm_fires.

## Wave G1 — GUI CLI-parity ProbeView panels (R4-05 slice, agent, self-contained)
4 read-only tabs via the council-weights template (6 wiring pts each; nav key / props / title / if-block / settings-exclusion-guard / main.rs handler):
- adr-browser (◆, app_shell:246, main.slint:516/904/3690/3172, main.rs:3974) — `neoth adr list --output json` (cli/adr.rs verified)
- mode-registry (⊡, app_shell:257, main.slint:519/905/3699/3173, main.rs:4016) — `neoth mode list --output json` (cli/mode.rs)
- bg-jobs (⊛, app_shell:224, main.slint:522/906/3708/3174, main.rs:4058) — `neoth jobs --bg --output json` (cli/jobs.rs)
- migrate-history (⊸, app_shell:269, main.slint:525/907/3717/3175, main.rs:4100) — `neoth migrate list --output json` (cli/migrate.rs)
Files: app_shell.slint, main.slint, main.rs (GUI only). GATE: pending (with hook wave).

## Wave G2 — R4-05 CLI↔GUI capability-parity drift guard (self-contained)
- `cli/parity_drift.rs` (`#[cfg(test)]`, declared cli/mod.rs:126): canonical capability INVENTORY — all 104 non-hidden clap verbs → `Gui("<nav-key>")` | `CliOnly("<reason>")`. Verbs enumerated LIVE from `Cli::command()` (auto-fresh, no hand-transcription); hidden (Internal/GuiStream) filtered via `is_hide_set()`. NAV_KEYS = 39 `root.nav()` keys from app_shell.slint.
- 3 guards: `every_cli_capability_is_triaged` (new verb w/o triage → fail + non-empty surface check), `inventory_has_no_stale_verbs` (removed/renamed verb → fail), `gui_targets_reference_real_nav_keys` (typo in nav-key → fail). Sibling of DOC-01 docgen drift infra.
- VERIFIED: 3/3 green FIRST RUN vs live clap tree → all 104 classifications match ground truth, every Gui() target maps to a real panel (no dangling "mapped-but-no-panel" gaps → mapped-set parity structurally complete). GATE_EXIT=0, 1 dead-code warning fixed (reason field consumed by empty-surface check).
- REVIEW (rust-reviewer, adversarial on guard MECHANISM): 1 HIGH — `Cluster` verb is `#[cfg(feature="cluster")]`-gated (default-on) but INVENTORY had it unconditional → (a) `--no-default-features` false-stale-fail, (b) forward bypass: a future cfg-gated verb added w/o a row is invisible to all 3 tests when its feature is off. FIXED: `Surface: Copy`; cluster row moved to `full_inventory()` cfg-gated push (symmetric in both configs); doc mandates `--all-features` CI + same pattern for future gated verbs. + floor guard (`live>50`) vs vacuous-empty enumeration. Other 3 concerns (is_hide_set scope, HashSet Borrow logic, alias non-enumeration) = no defect. Re-tested 3/3 green.

## Wave G3 — GUI text-overflow ("text rendert außerhalb der Rahmen") — 4 parallel agents, disjoint files
- Bug class: dynamic (data-bound) `Text{}` in a bounded container w/o `overflow:`/`wrap:` guard → renders outside its frame. Prior wave 6d0bf5a8 did 72; this pass covers the tail.
- Scanner `C:/Temp/scan_overflow.py` (brace-matched Text blocks, dynamic-binding heuristic, skips pure literals/@tr) → 116 candidates across 23 files (`C:/Temp/overflow_candidates.txt`).
- 4 agents (disjoint ownership): A=settings(26 elide/1 wrap/1 skip) · B=components+chat+main(18 elide/8 skip) · C=mesh+overview+tabs+loop(17 elide/2 wrap/10 skip) · D=15 small files(13 elide/22 skip). TOTAL **74 elide + 3 wrap = 77 fixes, 41 skip** over 18 files.
- Judgment quality: agents inspected the ENCLOSING element — self-sizing chips (`width = preferred-width + pad`) → SKIP (grows w/ content); capped counters (`9+`/`99+`), single glyphs (▾▸●○), spinner toggles (`running…`) → SKIP; bounded/explicit-width containers w/ unbounded IDs/paths/timestamps/`"label: "+data` → elide; multi-line prose (Matrix blurb, loop-status, ProbeView blurb) → wrap.
- VERIFIED: neothd-gui `cargo check` GATE_EXIT=0 (Slint compiler validates every property placement). 3 wrap cases all genuine multi-line. Dup-guard scan: 3 Text w/ both overflow+wrap = ALL pre-existing intentional wrap+tail-elide (activity:208, dreaming:339, overlay:162), none agent-introduced.

## Wave G4 — mesh dashboard: silent-bug triage + auto-refresh (roadmap HIGH·needs-rust, Codex-zone now mine)
- VERIFY-FIRST caught a STALE roadmap "silent bug": **peer expand/collapse** claimed "Rust never wires set_expanded_peer" — but the toggle now lives in Slint (main.slint:4001 `peer-expanded(id) => mesh-expanded-peer = (== id ? "" : id)`, in-out → MeshView expanded-peer:3994 → PeerCard.expanded:688 → detail if:298). Fully functional, no Rust needed. Marked resolved, NOT re-fixed.
- **Auto-refresh** (REAL): `refresh_mesh` fired only at startup+manual → dashboard went stale. BUILT: dedicated 30s polling thread (main.rs ~6054) after the startup one-shot, gated on `Arc<AtomicBool>`, self-terminates via `slint::invoke_from_event_loop(||{}).is_err()`. Toggle: `in-out mesh-auto-refresh-enabled` + `mesh-auto-refresh-toggled(bool)` (mesh.slint), "⏸/▶ Auto" NeothButton in header, forwarded through main.slint:857/3996 → `on_mesh_auto_refresh_toggled`. 5 edits/3 files.
- GATE: neothd-gui GATE_EXIT=0 (after 1 fix: `Ordering` not in scope at that point in main.rs → fully-qualified `std::sync::atomic::Ordering::Relaxed`). rust-reviewer on the thread: **no mechanism defects** — termination safe (sleep→probe→refresh; max consequence = one stale 4-probe set at shutdown then breaks; `invoke_from_event_loop().is_err()` is permanent, no Ok-oscillation), Relaxed sufficient (no data guarded), no panic path (weak.upgrade guard), no refresh overlap (synchronous on poll thread; manual+poll serialized by event loop).

## Wave G5 — GUI-roadmap verification sweep (Workflow wf_32375a53) + WIP-limit build
- Ultracode Workflow: 6 read-only verifiers (partitioned by file) + synth over all 55 open roadmap items. Result: **54 items → 4 STALE, 42 REAL, 8 PARTIAL**; 614k subagent tokens, 7 agents, 0 errors.
- STALE (already done, marked [x]): L53 ContextMenu-arch (3 menus already in main.slint), L206 ADR / L210 council / L214 bg-jobs (my Wave G1 panels). My G1 read-only panels correctly show PARTIAL vs roadmap items that also wanted write-actions (migrate-run, mode-browser, mcp-tool-call).
- **DECISIVE FINDING: nearly all 42 REAL items are `shared_files`** (route through main.slint/main.rs — context menus mount there per the arch rule, panels wire handlers there) → parallel-agent building NOT viable, must serialize. Only 2 truly-iso pure-slint `build_now_solo`: L128 (WIP-limit), L81 (operator-notes multi-line). Roadmap header agrees: "fast alles needs-rust + Codex-Zone; Claude-Lane = nur pure-slint+non-codex".
- **BUILT L128 (WIP-limit viz)**: KanbanColumn `in property <int> wip-limit` + `over-wip` bool (settings.slint:342); count shows "N/limit" + `Theme.warning` + 2px warning border when tasks.length>=limit; board defaults IN-PROGRESS:3, REVIEW:5. Pure-slint, settings.slint only. GUI gate GATE_EXIT=0.
- **BUILT L81 (operator-notes multi-line)**: new `NeothTextArea` component (components.slint — mirrors NeothLineEdit's bg-base box, focus-ring, stacked placeholder, but `single-line: false` + `wrap: word-wrap` TextInput for multi-paragraph steering notes); kanban comment composer (settings.slint) swapped NeothLineEdit→NeothTextArea. Enter=newline, Add button submits, callback `kanban-task-comment(string,string)` unchanged → zero Rust. GUI gate green. **build_now_solo lane now EXHAUSTED (L128+L81 done).**
- High-value serialize backlog (needs-rust, Codex-zone, one-at-a-time): credential panel, moral-core editor, inline-tool-approval, request-sync-real, task-priority/label, message-branch-nav, artifact-canvas, inline-patch-diff, groundtruth-write, rollback-apply, mesh-peer-ctx, composite-health-bar (all HIGH).

## Wave G6 — serialize backlog: composite cluster health bar (roadmap L170, first serialize item)
- First shared-file feature after the iso-lane exhaustion. Additive to refresh_mesh (I know it from G4 auto-refresh). 4 edits/3 files:
  - main.rs refresh_mesh (~14878): `healthy_peers = peer_rows.filter(reachable && staleness_secs < 60).count() as i32` before the peer_rows move → `set_mesh_healthy_peer_count`.
  - mesh.slint: `in property <int> mesh-healthy-peer-count` on MeshView + a fleet-health bar under the header (guarded `if mesh-peers.length > 0`): "N/M healthy" + proportional fill (width = track.width * healthy / total), green(all)/amber(>=half)/red(<half).
  - main.slint: `in-out property <int> mesh-healthy-peer-count` + MeshView binding.
- GATE: neothd-gui GATE_EXIT=0. rust-reviewer: 1 MEDIUM (division/color-threshold/type = no defect) — `staleness_secs` defaults `unwrap_or(0)` for display, so a reachable peer with NO gossip snapshot looked "0s fresh" → falsely counted healthy. FIX: compute `healthy_peers` from `snap.peers` joined with `swarm_nodes`, `map(age<60).unwrap_or(false)` (no snapshot ⇒ unhealthy), BEFORE the model move — leaves the per-card displayed staleness untouched (mesh.slint:328 "STALE Ns"). Re-gate green.

## Wave G7 — mesh peer right-click context menu (roadmap L73, build-agent + review)
- Build-agent (single, owns 3 files, mirrored kanban-ctx pattern): PeerCard `context-menu` callback + `pointer-event(ev)` on det-ta (right-up → fire w/ peer-id+coords) mesh.slint; MeshView `mesh-peer-ctx` re-emit; main.slint 4 `mesh-peer-ctx-*` props + ContextMenu block (Force Sync · Copy ID · Remove Peer) mirroring kanban-ctx clamping; main.rs `on_mesh_peer_copy_id` (arboard) + `on_mesh_peer_revoke`. Actions all pre-existing (`cluster revoke` daemon subcmd, sync reuses on_mesh_peer_sync_clicked). GATE green.
- MY FIX before review: agent's revoke used `run_neothd_probe` (merges stderr, drops exit status) + always "success" toast → a FAILED revoke showed green. Rewrote to spawn directly (which_neothd()+spawn_neothd_plain), check `o.status.success()` → success/error toast, refresh_mesh only on success. peer-id passed as single argv (injection-safe). Re-gate green. rust-reviewer (slow but COMPLETED, ~214s): pointer-event/ctx-state/clipboard/destructive-UX = clean; 2 real defects FIXED:
  - HIGH mesh.slint:296 — ctx-menu used element-local `self.mouse-x` → menu pinned to content corner. FIX: `self.absolute-position.x + self.mouse-x` (window-absolute) matching the kanban-ctx source (settings.slint:277); kept the sidebar/header clamp identical to kanban rather than the reviewer's remove-offsets (consistency with the shipped reference; if kanban has a latent offset both fix together).
  - MEDIUM main.rs:6119 — `.output().ok()` dropped spawn io::Error → wrong "binary not found" on perm/OOM. FIX: `.inspect_err(warn).ok()`.
  Re-gate green. Destructive Remove Peer = reviewer verdict "not a footgun" (deliberate 2-step, danger-flagged, re-pairable) → no confirm needed.

## Wave G8 — chat channel right-click context menu (roadmap L37, build-agent + review)
- Build-agent mirrored the CORRECTED mesh-peer-ctx template (absolute-position coords baked in), no deviations: ConversationItem `context-menu` callback + pointer-event(right-up) chat.slint; ChatView `channel-ctx` re-emit; main.slint 4 `channel-ctx-*` props + ContextMenu (Remove Channel · Copy Name); main.rs `on_channel_copy_name` (arboard). Remove wires to the PRE-EXISTING `on_channel_remove`/`channel-remove` (not re-registered).
- Identity: menu passes `root.label` (channel display name) → `channel remove` → `resolve_operator_channel`/`resolve_channel_id` (accepts canonical IDs + aliases). Worst case if a label isn't a valid alias = "unknown channel" error toast (resolve errors, does NOT remove the wrong channel). GATE green. rust-reviewer: pointer-event/ctx-state/clipboard clean; 2 defects FIXED:
  - HIGH main.rs:6548 — `parse_channel_removed(stdout, &name)` compared daemon-canonical "telegram" == passed "Telegram" case-sensitively → EVERY successful remove showed "remove response invalid" (the remove itself worked via `canonical_token` lowercasing). FIX: `&name.to_ascii_lowercase()`. Benefits Settings' path too.
  - MEDIUM main.slint — comment claimed a confirm but Remove fired immediately; channels hold credentials (not in-app reversible). FIX: Remove now opens a `ConfirmDialog` (danger, "Deletes <name> and its stored credentials…") → confirmed→channel-remove. (mesh revoke stayed no-confirm: peers hold no secret — reviewer's distinction.)
  - Re-gate green. NOTE (follow-up): `chat-channels` (main.slint:977) is a hardcoded stub `["CLI","Telegram","WhatsApp"]` — no `set_chat_channels` in Rust; parallel-indexed with unread-counts so wiring to real state needs care. Copy/Remove operate on these labels (resolve_channel_id maps them); real-state wiring deferred.

## Wave G9 — ctx-menu coord DOUBLE-OFFSET fix (follow-up task_474bf23d, landed via race)
- CONFIRMED the peer-ctx reviewer's HIGH was RIGHT and my "mirror kanban for consistency" was WRONG: Slint `absolute-position` is already window-relative (Context7 doc + shell layout `HorizontalLayout{Sidebar, VLayout{TopBar, content}}` puts content-origin at (sidebar-w, topbar-h) already), and the ContextMenu overlays the full window → adding sidebar-w/68px = DOUBLE offset. Kanban had the latent bug too; I propagated it to mesh + channel.
- FIX (task, all 3 clamps main.slint): `menu-x/menu-y: min(root.<ctx>-x, root.width-Npx)` — offset removed, edge-clamp only. Verified at HEAD: kanban(4121/4122), mesh-peer(4158/4159), channel(4179/4180) all offset-free, 0 leftover `+ Theme.sidebar/header`.
- ⚠ GIT-RACE: the coord-fix task ran in the SAME main checkout concurrently with my channel-ctx build; a `git add -A`-style stage swept its main.slint edits into MY commit `04be10c7` (with channel-ctx). NET RESULT clean: 04be10c7 (== origin/main) combines both, GATE_EXIT=0, my channel-ctx handlers intact, task's worktree branch has no dangling commits, reflog linear. Reconcile = already-on-main (option a). Lesson: concurrent sessions in one checkout + git-add-all = cross-contamination; result happened to be consistent + compiling. New memory: [[neoth_slint_abspos_window_relative]].

## Wave G10 — groundtruth ADD write action (roadmap L93 slice, first write on a probe-panel)
- Bounded, non-destructive slice of L93 (full add/revoke/state/contradictions deferred — revoke is destructive + security-sensitive, better fresh-context). Wrapped the read-only groundtruth ProbeView (main.slint:3672) in a VerticalLayout with a NeothLineEdit + Add button → `gt-add-clicked(text)`.
- main.rs `on_gt_add_clicked` (~3804): `groundtruth add <stmt.trim()> --output json` (single argv → injection-safe), status-checked (`.status.success()` → success/error toast, `.inspect_err().ok()` on the add surfaces spawn failure), empty/whitespace guarded (slint `t!=""` + rust `stmt.trim().is_empty()`), then re-runs `groundtruth list` to refresh gt-output. Add is non-destructive → no confirm. GATE green. rust-reviewer: injection/status/empty/weak/bypass CLEAN; 1 MEDIUM (refresh  swallowed spawn err → added inspect_err), 1 LOW (no confirm — but mirrors CLI friction-free Add, left). Re-gate green. ADD + REVOKE built. REVOKE () = exact copy of the reviewed add-handler structure (which_neothd+spawn+status-check+refresh, injection-safe single argv, inspect_err) composed with the reviewed channel-remove ConfirmDialog (danger, audit-preserving body). State/Contradictions deferred.

## Wave G11 — collected: chat-channels real-state wiring (task_7706b33f worktree)
- User directive: worktree-tasks abholen. task_7706b33f (chat-channels stub → real) ran in worktree festive-wilbur, finished IDLE leaving its work UNCOMMITTED. Collected via  onto main (apply_channels +23, disjoint from my groundtruth edits — clean apply, gate green).
- The wiring: ;  (length-matched); active-index clamped if the list shrank. Verified sound (the index-sync the channel-ctx reviewer flagged). Makes the G8 channel-ctx menu operate on REAL canonical channel ids, not the hardcoded stub. Resolves the chat-channels-stub follow-up.

## Wave G12 — catalog Rebuild write action (roadmap R4-05 parity slice)
- The catalog panel blurb said refresh is CLI-only. Wrapped the read-only catalog ProbeView with a "Rebuild from providers" button → `catalog-refresh-clicked` → main.rs `on_catalog_refresh_clicked`: `neoth catalog refresh --output json` (status-checked success/error toast, inspect_err), then re-lists. Non-destructive, no user input (injection-N/A). Exact copy of the reviewed groundtruth-add handler structure. GATE green. Committed on the vetted pattern (no separate review).

## Wave G13 — R4-05 campaign (canonical tracker): per-step CLI-verify → build → gate → review loop
- Operator directive: complete ROAD_TO_1_0_GOLD.md, no shortcuts, research→wiring→debug agent loops. Adoptions confirmed done (WS-G/WS-I 0 open, 59 QUELLEN dirs). 14 open = R4 release epics; 4 externally-gated (R4-01 signing, R4-02 pkg-mgr accounts, R4-08 clean-machine runners, R4-12 mobile toolchains) — scaffolding-only without operator certs/accounts/hardware. Code-buildable: R4-05/06/07/09/13.
- **Per-step CLI verification caught the RESEARCH_GOLD_R4 doc over-claiming CRUD** (recorded as a correction block in that doc): `neoth stop`/`mode set`/`credential store-delete`/`slash add-remove`/`hooks add-remove-run`/`tweaks set-unset` DO NOT EXIST → building them = dead buttons R4-05 forbids. Real writes: mcp call, groundtruth add/revoke (done), quota set-cap.
- **MCP Tools+Call** (build-agent): wrapped the read-only mcp ProbeView (main.slint:3659) with server/tool/args inputs + List-tools/Call-tool buttons → `on_mcp_tools_clicked`/`on_mcp_call_clicked` (main.rs ~3722/3778): `mcp tools <server>` / `mcp call <server> <tool> --args <json>`, each arg a separate argv (injection-safe), status-checked, mirrors gt-add. GATE green. rust-reviewer: injection/empty-guards/thread-weak/consent-bypass all CLEAN; 1 MEDIUM (both `None` arms said "not on PATH" even when the located binary FAILED to spawn — misleading, real error only in the inspect_err log). FIXED: honest message covering both modes ("not on PATH, or failed to spawn — see logs"). (NOTE: same None-arm pattern in gt-add/revoke/catalog handlers — message-only, follow-up.) Re-gate green.

## Wave G14 — R4-05 read-only parity tabs: credentials + slash (build-agent) — `2d534666`
- Build-agent audited the 4 target read-only surfaces: hooks + tweaks tabs ALREADY EXISTED (research doc stale — NavBtn+props+handler already wired), so it added credentials + slash. 6 wiring pts each (NavBtn app_shell + props/title/if-block/settings-guard main.slint + main.rs handler), exact catalog-tab idiom.
- credentials = `neoth credential list --output json` (KEY NAMES ONLY, no secret values — by CLI design, no leak). slash = `neoth slash list --output json`. Both pure read-only ProbeView (no user input → no injection surface, no write). GATE green. Proportionate rigor: pattern-mirror of proven catalog/hooks tabs + verified read-only (no adversarial review — no write/destructive/input surface).
- ⚠ Agent STALLED (0 edits ~6 turns, large-Write attempt) → `SendMessage` nudge "small Edit hunks" unstuck it (known stall pattern). ⚠⚠ RE-CONFIRMED recurring hazard: `python -c` with triple-quotes/backticks in bash heredoc breaks (unterminated-string / command-substitution) — ALWAYS use Edit tool or a `.py` file for doc edits, NEVER inline python -c.

## Wave G15 — R4-05 quota set-cap editor + step-15 nav-reachability verified — (self-built)
- STEP 15 (research doc "cron/n8n/selfreprog not wired to nav"): STALE — all reachable (automation→CronView main.slint:3932, n8n→N8nView:3964, evolve→SelfImproveView:4006, selfdev→SelfReprogramView:4024). Marked done, nothing to build.
- **quota set-cap editor**: wrapped the read-only quota ProbeView (main.slint:3822) with provider+cap inputs + "Set cap" button → `on_quota_setcap_clicked` (main.rs after 4323): `neoth quota set-cap <provider> <cap> --output json` (positional argv, injection-safe; cap-string → CLI parses u32, non-numeric → error toast), status-checked (honest None-arm per the mcp reviewer fix), refresh status after. Non-destructive (reversible cap) → no confirm. GATE green. Near-exact copy of reviewed gt-add/mcp-call pattern → committed on vetted pattern.
- update-check button deferred: `neoth update` check-invocation form unclear (no clean UpdateAction::Check verb found) — verify before building (no shortcut = no guessed command).

## Wave G16 — R4-05 slash quick-entry chips + R4-05 BUILDABLE-COMPLETE (self-built)
- /critic /background /quit had no GUI trigger. Added a pure-slint quick-entry chip row above the chat composer (chat.slint ~715): each chip sets `root.draft = cmd + " "`, the existing slash-dispatch (slash-active) runs it on send. Discoverability, not auto-execute (operator still sends). No rust, no injection (fixed strings), non-destructive. GATE green.
- **R4-05 BUILDABLE PARITY = COMPLETE.** Built this session: mcp tools+call, credentials tab, slash tab, quota set-cap, groundtruth add/revoke, catalog rebuild, slash quick-entry. ALREADY-DONE (research doc stale): hooks/tweaks tabs, auto-update card + check-now (on_autoupdate_check_clicked main.rs:3637, NOT dead), mcp list, undo read-only (Doctor), cron/n8n/selfreprog/self-improve nav, memory-graph export, parity_drift.rs drift test (Wave G2). UNBUILDABLE-AS-DESCRIBED (no CLI verb → would be dead buttons R4-05 forbids): `neoth stop`, `mode set`, `plugin policy set`, credential store/delete, slash add/remove, hooks add/remove/run, tweaks set/unset. The R4-05 EPIC box stays open (release-gate acceptance = machine-tested + drift tests + broader), but the GUI-parity build work is done + honest (no fake CRUD).

## GATE + TEST (build phase D1/G1 closed)
- daemon `cargo check -p neoth --tests` GATE_EXIT=0, 0 warnings.
- neothd-gui GATE_EXIT=0.
- tests 313/313: hooks 82, tokens::budget 28, smart_approve 33, dispatch_loop 49, session_sort_cron 20, cron::runner 25, serve_pipeline 23, n8n_api 53.
- BUG-HUNT during test phase (build-log localized): 3 PRE-EXISTING cron::runner failures from the Wave-3 salvage (never ran cron::runner after that merge) — NOT this wave:
  - default_cron_revoked / per_job_cloud_provider: salvage wrapped the consent error in `.with_context()` so `error.to_string()` shows only the top job-context; the provider name + "revoked" wording live in the source chain. Security intact (fail-closed, calls==0). Fix: assert on `format!("{error:#}")` (full chain) with current wording.
  - run_briefing_gated_emit_path: salvage tightened consent so the Emit path reaches the provider; the 1-word CountingProvider stub then correctly FAILS the briefing quality gate (calls==2 via initial+regen, success=false). Test intent = delegation. Fix: pin a LocalQwen config (consent-free) + assert delegation (calls>=1) + success==false (stub can't pass quality). 
