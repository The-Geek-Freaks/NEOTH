# BUILD LOG — 2026-07-17 (build-all-then-hunt strategy)

Strategy: build every loop-buildable open ROAD item + emergent sub-items FIRST,
recording exact wiring (file:line, what/how) here, THEN one consolidated
test + bug-hunt pass using this log for fast localization.

Gate policy during build phase: `cargo check -p neoth --tests` (compile only)
after each wave. Focused tests + bug-hunt run once the build phase closes.

Scope excluded (multi-session release epics): GOLD-R3-10, R4-01/02/04/07/08/09/10/12/13.

---

## Wave D1 — daemon code items

### BUG-W2-P1-HOOK-CHANNEL-PARITY / HOOK-ONCE-PARITY
- root cause: `once` never read by dispatcher; each caller does TOCTOU check-then-insert around a synchronous set → concurrent channel turns / repeated cron ticks both fire. serve_pipeline PreProviderCall discards FilteredBlocks (no restore path).
- built (hooks/, agent): `SessionOnceGuard(Arc<Mutex<HashSet>>)` dispatcher.rs:529 (Clone=Arc-share); `StageOnceResult{outcome, filtered_blocks, skipped_once}` :562; `run_stage_with_once_guard(stage, body, hooks, invoker, fail_fast, &guard) -> Result<StageOnceResult>` :609 (atomic claim-before-effect under mutex, lock never across await); `restore_blocks(body, &[FilteredBlock])` block_filter.rs:130. Re-exported hooks/mod.rs:20-22. Old fns unchanged. Tests: once_guard_concurrent_claims_allow_exactly_one_fire / once_guard_sequential_second_call_is_skipped / post_provider_call_with_once_guard_and_block_filter_restoration.
- CALLER WIRING (delegated to wiring agent; owns chat.rs/serve_pipeline.rs/cron/runner.rs):
  - chat.rs ~5040 HashSet→SessionOnceGuard; run_hook_stage param; delete pre-filter ~5403-5437 + post-insert ~5470; call run_stage_with_once_guard + emit HOOK_SKIPPED_ONCE per skipped_once name; 5 sites (2246,2296,3720,5075,5232) pass &once_guard.
  - serve_pipeline.rs 812 Arc<Mutex<HashSet>>→Arc<SessionOnceGuard>; PreChannelIngress ~1047 / PreEgress ~620 / PreProviderCall ~2320 → run_stage_with_once_guard, KEEP result.filtered_blocks as pending_blocks; ADD PostProviderCall after LLM reply: restore_blocks(&reply,&pending_blocks)+run stage.
  - cron/runner.rs 7 sites (179,1807,1851,1910,1964,2005,2043): per-job SessionOnceGuard, run_stage fn-ptr → closure mapping StageOnceResult→outcome.

### FOLLOW-UP-W3-TOKENCAP-DAEMON-DEGRADE — BUILT (agent, self-contained)
- `finalize_daemon_request(prompt, Option<system>, cap) -> Result<DaemonBudgetOut{system,prompt,total_tokens}, &str>` tokens/budget.rs ([A(system?),E(prompt)], enforce_budget_to_fit, fail-close if A/E can't fit). Tests: fits_unchanged / over_cap_e_only_with_a_protected_returns_err / a_and_e_cannot_fit_returns_err.
- routed: serve_tasks.rs:2046 (OMI; cap=reload_controller.latest().tokens.max_per_request), n8n_api/handlers.rs:370 (cap=config.tokens.max_per_request), session_sort_cron.rs:176 (+cap param thru group_titles/run_session_sort_pass; spawn loop 570-585 effective_cap). Caps = existing reload/config path.

### FOLLOW-UP-W3-SMARTAPPROVE-DUP-TOOL-CLIENT — BUILT (agent, self-contained)
- smart_approve.rs initialize_server ~404: `has_grants=!verdicts.is_empty()`; seed_verdicts unconditional (seal stays); insert_live only if has_grants; else client drops → McpClient::Drop → child.start_kill() (kill_on_drop client.rs:243). Test: duplicate_tool_seal_no_client_retained_and_confirm_fires.

## Wave G1 — GUI CLI-parity ProbeView panels (R4-05 slice, agent, self-contained)
4 read-only tabs via the council-weights template (6 wiring pts each; nav key / props / title / if-block / settings-exclusion-guard / main.rs handler):
- adr-browser (◆, app_shell:246, main.slint:516/904/3690/3172, main.rs:3974) — `neoth adr list --output json` (cli/adr.rs verified)
- mode-registry (⊡, app_shell:257, main.slint:519/905/3699/3173, main.rs:4016) — `neoth mode list --output json` (cli/mode.rs)
- bg-jobs (⊛, app_shell:224, main.slint:522/906/3708/3174, main.rs:4058) — `neoth jobs --bg --output json` (cli/jobs.rs)
- migrate-history (⊸, app_shell:269, main.slint:525/907/3717/3175, main.rs:4100) — `neoth migrate list --output json` (cli/migrate.rs)
Files: app_shell.slint, main.slint, main.rs (GUI only). GATE: pending (with hook wave).

## GATE + TEST (build phase D1/G1 closed)
- daemon `cargo check -p neoth --tests` GATE_EXIT=0, 0 warnings.
- neothd-gui GATE_EXIT=0.
- tests 313/313: hooks 82, tokens::budget 28, smart_approve 33, dispatch_loop 49, session_sort_cron 20, cron::runner 25, serve_pipeline 23, n8n_api 53.
- BUG-HUNT during test phase (build-log localized): 3 PRE-EXISTING cron::runner failures from the Wave-3 salvage (never ran cron::runner after that merge) — NOT this wave:
  - default_cron_revoked / per_job_cloud_provider: salvage wrapped the consent error in `.with_context()` so `error.to_string()` shows only the top job-context; the provider name + "revoked" wording live in the source chain. Security intact (fail-closed, calls==0). Fix: assert on `format!("{error:#}")` (full chain) with current wording.
  - run_briefing_gated_emit_path: salvage tightened consent so the Emit path reaches the provider; the 1-word CountingProvider stub then correctly FAILS the briefing quality gate (calls==2 via initial+regen, success=false). Test intent = delegation. Fix: pin a LocalQwen config (consent-free) + assert delegation (calls>=1) + success==false (stub can't pass quality). 
