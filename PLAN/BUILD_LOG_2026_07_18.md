# BUILD LOG — 2026-07-18 (GUI TOP lane — Claude design session)

Lane split: Claude owns DESIGN/GUI lane this session (Alex informed Codex).
Codex serialize backlog stays untouched: credential panel, moral-core editor,
inline-tool-approval, request-sync-real, task-priority/label, message-branch-nav,
artifact-canvas, inline-patch-diff (chat), rollback-apply.

Research inputs (5 parallel agents, 2026-07-18): GUI inventory deep-scan,
plan-state extraction, CLI↔GUI parity (parity_drift.rs 134 verbs), backend
capability scan (buddy/kanban/cluster), best-in-class UX research (Claude
Desktop / ChatGPT+Codex / Cursor 3 / Linear / Raycast / kanban tools / pet UX).
Consolidated backlog lives in GUI_TOP_TIER_PLAN.md (updated this session).

Gate policy: `_gui_check.bat check -p neothd-gui -j2` per wave; focused GUI
tests at block end. Never run GUI builds concurrent with other gates (G19).

---

## Wave W0 — CEILING BROKEN: Slint codegen stack fix + R4-09 step 4 ✅ (d455efc5)
- `neothd-gui/build.rs`: slint_build::compile now runs on a 256 MiB stack
  thread (default 8 MiB was the G19 STATUS_STACK_OVERFLOW cause).
- Proof: re-applied the EXACT reverted G19 edit (5 if-blocks: Ctrl+6..9 nav +
  Ctrl+B sidebar toggle, main.slint ~3080) → gate green in 2m13s.
- Ctrl+6..9 = agents · automation · mesh · doctor (stable targets, bypass
  sidebar complexity gating like the palette). Ctrl+B toggles
  `Theme.sidebar-collapsed` (theme.slint:67 in-out). Cheatsheet updated.
- CONSEQUENCE: main.slint growth is no longer blocked; the "split main.slint
  first" precondition for the serialize backlog is void. Long-term split still
  sensible for compile time, not for correctness.
