# BUILD LOG — 2026-07-18 (GUI TOP lane — Claude design session)

Lane split: Claude owns DESIGN/GUI lane this session (Alex informed Codex).
Codex serialize backlog stays untouched: credential panel, moral-core editor,
inline-tool-approval, request-sync-real, task-priority/label, message-branch-nav,
artifact-canvas, inline-patch-diff (chat), rollback-apply.

Research inputs (5 parallel agents, 2026-07-18): GUI inventory deep-scan,
plan-state extraction, CLI↔GUI parity (parity_drift.rs 134 verbs), backend
capability scan (buddy/kanban/cluster), best-in-class UX research (Claude
Desktop / ChatGPT+Codex / Cursor 3 / Linear / Raycast / kanban tools / pet UX).
Consolidated backlog lives in GUI_TOP_TIER_PLAN.md (updated this session).

Gate policy: `_gui_check.bat check -p neothd-gui -j2` per wave; focused GUI
tests at block end. Never run GUI builds concurrent with other gates (G19).

---

## Wave W0 — CEILING BROKEN: Slint codegen stack fix + R4-09 step 4 ✅ (d455efc5)
- `neothd-gui/build.rs`: slint_build::compile now runs on a 256 MiB stack
  thread (default 8 MiB was the G19 STATUS_STACK_OVERFLOW cause).
- Proof: re-applied the EXACT reverted G19 edit (5 if-blocks: Ctrl+6..9 nav +
  Ctrl+B sidebar toggle, main.slint ~3080) → gate green in 2m13s.
- Ctrl+6..9 = agents · automation · mesh · doctor (stable targets, bypass
  sidebar complexity gating like the palette). Ctrl+B toggles
  `Theme.sidebar-collapsed` (theme.slint:67 in-out). Cheatsheet updated.
- CONSEQUENCE: main.slint growth is no longer blocked; the "split main.slint
  first" precondition for the serialize backlog is void. Long-term split still
  sensible for compile time, not for correctness.

## Wave W1 — verify-first sweep + I6/I7 ✅
- VERIFY-FIRST: 5/6 inventory-agent "dead-end bugs" were FALSE (channel-ctx,
  selfreprog-apply, WAL-sidebar/timeline, memgraph/groundtruth, dreaming-refresh
  all fully wired at HEAD — receipts in GUI_TOP_TIER_PLAN Wave I). Lesson
  re-confirmed: agent TOP-N lists need per-item code verification before build.
- I6 (cron.slint + main.slint): Run button `enabled: !daemon-live` +
  TooltipArea "Managed by the live daemon — runs on its schedule";
  `daemon-live: root.daemon-state == "live"` at the CronView site.
- I7 (gui_stream.rs + buddy_activity.rs): FOLLOW_TYPES +10 opcodes
  (0x25,0x65,0x9F,0xAE,0xAF,0xD9,0xDB,0xDC,0xF0,0xF2); mapping fallback/
  consent/memory-tick/audit/secured; new GuiActivity::QuotaBreached →
  ("problem","disk quota hit"). NOT subscribed: provider hot-lane 0x20..0x23.
  New test every_followed_type_maps (subscription ⊆ mapping). Tests 2/2 + 6/6.
- Gate: GUI_GATE_EXIT=0 (3m42s full slint recompile).

## Wave W2 — kanban enrichment + buddy idle-variety + SAFE_GAPS sweep ✅
- I10 (agent-built, kanban.rs/main.rs/panel_logic.rs/settings.slint):
  GuiBoardTask +9 serde-default fields (warm channel), CodingTaskJson +7,
  KanbanTaskRow +7 (type-chip/worker/tests/tests-failing/eta/parent-ref/
  has-patch), chip row on card, warm+cold byte-equivalent. ⚠ eta_ns =
  DURATION from started_ns (store.rs test), format_eta_at(now) injectable.
  Serde-field bug-class hit again: 7 test literals needed ..Default::default()
  (derive Default added).
- I11: proportional summary bar above board (stretch per column count).
- I8/I9 (agent-built, buddy.slint): 20s idle cycle (look-around/double-blink/
  yawn/breathe), amp 0.45 mode 1, static mode 0; working/analyzing overlay
  entrance-fade; dock FocusScope Enter/Space/Menu + circular focus ring
  (dock-hue mirror). Caption-crossfade + other overlay fades VERIFIED-DONE.
- I16 sweep (main thread, 11 files): ConfirmDialog ×4 (chat delete, cron
  remove, mesh prefer-A/B, selfimprove rollback — local property-armed
  pattern); blinking stream caret ▋↔▏; palette keyboard-legend footer;
  app_shell font-token sweep (7 raw px → tokens); EntranceFade contract on
  ChatView/AgentsView/MemoryGraphView/LoopPanel/Resources/Probe/DoctorView;
  toast lifetime countdown hairline (I17 partial).
- Gates: neoth check --tests EXIT=0 (3m13s), neothd-gui check EXIT=0 (4m54s),
  tests 13+6+2 via _test_wave2.bat TEST_EXIT=0.

## Wave W3 — I13 BG-Jobs structured panel + submission ✅
- cli/jobs.rs run_bg_job: JSON ack {action:"jobs_run", started, id, pid,
  log_path} when --output json (additive; table path unchanged).
- NEW ui/bgjobs.slint: BgJobRow/BgJobCard/BgJobsView (run form + cards +
  EmptyState + RefreshFooter); main.slint bg-jobs ProbeView replaced,
  +4 root props, +1 callback, BgJobRow exported.
- gui_action.rs BgRunAck (deny_unknown_fields, verify action/started/id);
  main.rs: refresh handler now also fills bg_jobs_model via
  panel_logic::parse_bg_jobs (2 tests), submit handler fail-closed +
  success/warn toast (id, pid, log) + auto re-list.
- Gates: neoth 1m57s EXIT=0; neothd-gui EXIT=0 (0 warnings after log_path
  used in toast); tests 13+6+2 regression + 2 new = green.
- Parallel: hallmark installed (~/.claude/skills/hallmark, deep-read: SKILL.md
  + references verified real); design-polish workflow wf_978f487b-45f running
  (4-lens audit → punch list → worktree apply → adversarial verify).

## Wave W4 — I14 slash browser + hallmark polish merge ✅
- I14: SlashCmdsView/SlashCmdCard in tabs.slint (Led on/off, source chip,
  description); main.slint slash ProbeView replaced; parse_slash_cmds + 2
  tests. Read-only by design (no slash add/remove verbs → no dead buttons).
- HALLMARK LANE (workflow wf_978f487b-45f, 7 agents): 4-lens audit → 30-item
  punch list → worktree apply (branch worktree-wf_978f487b-45f-6, 8 ui files,
  73+/75-) → adversarial verify: PASS all checks, 0 reverts. Highlights:
  focus rings snap instantly + constant border-width (WCAG 2.2), bounce
  easing (0.34,1.56) → sovereign curve (0.16,1,0.3,1) on all 4 rails,
  emoji glyphs 💬📎 → geometric ▷⊕, card drop-shadow accent-glow → neutral
  4px, ink scale cool-anchored (+0.005 chroma) + ink-0 #f4f4f5 (no pure
  white), semantic color drift fixes (placeholder/hover/badge tints),
  mesh 9px → font-size-2xs, NeothLineEdit/TextArea instant focus-in state.
  Post-merge fix: ink-500/600 value inversion swapped (verify finding).
- NeothButton pressed-state punch item SKIPPED by apply agent (no bg-rect
  element) — re-queued as follow-up with correct element anatomy.

## Wave W5 — H13 space-peek + empty-state family + D3/I17 slices ✅
- H13 (agent, settings.slint only): hover-move callback chain (pointer-event
  move) Card→Column→Board, zero-height board FocusScope (Space/Esc, reject
  else), 520px overlay w/ id/title/hemisphere/column/I10-chips, scrim-dismiss.
  Pure Slint — no Rust wiring.
- I18 (agent): loop/dreaming/n8n/calendar bare-text empties → EmptyState
  (∞/☽/⇶/◷); transient loaders + configured-state cards deliberately kept.
- Main thread: NeothButton pressed-state (fill deepens on touch.pressed) +
  constant 2px border; ComplexitySeg constant border; theme toggle FocusScope
  (Enter/Space) + focus ring; push_toast max-3 cap.
- ⚠ D4 finding: Slint strings have NO contains/starts-with — pure-Slint
  type-to-filter impossible; must filter the MODEL in Rust. Cron filter
  attempt reverted; D4 stays open with that constraint noted.
- Gates: GUI EXIT=0, tests 13+6+2 green.

## Wave W6 — F2 durable sync-state surface ✅
- Backend existierte komplett (`cluster sync-state --output json`,
  durable_sync::MeshPeerStatus) — reine GUI-Fläche.
- panel_logic::parse_mesh_sync (+2 Tests): peer_pk→16-char-trunc, pending
  faltet attempts ("121 (3×)"), cursor "seg-0007.wal:4096", Optionals → "-".
- mesh.slint: MeshSyncRow struct + SYNC STATE Card (mono-Tabelle: PEER/ACKED/
  PENDING warn-amber/IN-NEXT/CURSOR/REQUEST audit-cyan + last-error
  consent-Zeile); versteckt bei 0 rows. main.slint +prop/forward/export.
- main.rs refresh_mesh: +1 Probe, Modell gesetzt.
- Gate EXIT=0, Tests 2/2.

## Wave W7 — H16 batch-select + bulk actions ✅
- settings.slint (agent): KanbanTaskRow +selected; card Ctrl+Click via
  pointer-event (left-up+ctrl → select-toggled, plain left-up → detail;
  H13/ctx-menu unberührt), amber batch-Border/Tint; Bulk-Bar (count-gated,
  →todo/in_progress/review/done + Archive/Block danger + Clear).
  Agent-Fix von mir: accessible-label brauchte accessible-role.
- main.slint: root-Props/Callbacks + SettingsView-Forwards.
- main.rs: KANBAN_SELECTION LazyLock<Mutex<KanbanSelection>> (panel_logic,
  +Test); apply_kanban_snapshot stampt selected + retain_known + count;
  restamp_kanban_selection (Modelle in-place, ohne Refetch); bulk-move-Loop
  über KanbanMoveAck mit Teilfehler-Report, danach clear+refresh.
- Beide KanbanTaskRow-Literale +selected:false (Serde-Feld-Klasse, via
  Python-Patch).
- Gates: GUI EXIT=0; Tests 13+6+2 Regression + kanban_selection 1 = grün.

## Wave W7 — Lost-Features-Archäologie ✅
- 6-Korpora-Dig (Workflow wf_bfbcf7a3-158, nach Prozess-Kill resumed): 79
  geprüfte Rohkandidaten. Verify-Phase des Workflows hing (Schema-Retry-Verdacht,
  880KB-Transkripte ohne Result) → gestoppt, Kandidaten aus journal.jsonl
  gerettet, Verify als 4 schlanke Klartext-Agenten neu gefahren.
- **Integrity-Korrektur (ersetzt die ursprüngliche 53/30-Zählung):** 79 =
  52 CONFIRMED_LOST + 8 ALREADY_BUILT + 11 ALREADY_TRACKED +
  3 REJECTED_STANDS + 5 semantisch zusammengeführte DUPLICATE-Kandidaten.
  Die 52 kanonischen Recovery-Zeilen sind exakt 23 P1 + 29 P2. Das vollständige
  Kandidaten-Ledger und der CI-Contract verhindern erneuten Zähler-/Mapping-Drift.
- Deliverable: PLAN/LOST_FEATURES_1_0_RECOVERY.md — P1/P2 mit file:line-
  Belegen, 7 Research-Vertiefungsaufträge, @Codex-Zuweisungssektion (13
  Backend-Items), Killed-Archiv gegen Wieder-Ausgraben.
- Top-Funde: ADR-009 INTENT/RESULT-Paare (5 Effektklassen), Mirror-Refusal
  Stages 2-6, Wizard-IPC-Stub (SURF-01), HLC-Ordering, Trust-Ledger,
  Proactive-Delivery-Routing (Stubs existieren!), plans/001-003 mit 65
  untracked Done-Criteria.

## Wave W8 — GOLD-LF-P1-03 external-output boundary ✅
- Kanonische Sanitizer-Grenze gegen Terminal-Controls, Zero-Width-Splitting,
  moderne Credential-Familien sowie snake/camel/kebab JSON-Credential-Felder;
  CRLF bleibt stabil, einzelnes CR wird entfernt, Guard-Sigils können nach
  Deobfuscation nicht reaktiviert werden.
- MCP: Raw-Byte-Accounting bleibt vor der Grenze, Erfolgs-WAL metadata-only;
  Text/Fehler/MIME werden danach saniert, Bilder strikt validiert und aus dem
  Modelltext ausgelassen. Persistentes File-CCR schreibt atomar owner-only und
  rekonstruiert cross-process genau einen vertrauenswürdigen äußeren MCP-Block;
  peer-kontrollierte innere Markdown-Fences und Guard-Closer werden defanged.
- Coding: Provider-/Git-/Cargo-Diagnosen, Retry-/Testlogs und
  PATCH_APPLY_FAILED-WAL werden am Producer saniert. Ausführbare Patches werden
  byte-exakt geprüft: jede durch Control-/Secret-Scan nötige Änderung blockiert
  Persistenz und Apply; saubere Patches werden unverändert atomar owner-only
  persistiert.
- Recall/Persistenz: Agent-Turns, Provider-Sessiontitel und Dream-Summaries
  sanitizen vor Persistenz; Legacy-Transcript-/Session-/Recall-/Code-Map-Zeilen
  erneut auf Egress. Operator-Source-Turns bleiben absichtlich byte-exakt.
- Adversarial review fand sechs reale Lücken (camel/kebab JSON, standalone CR,
  Stage-1-Providerfehler, Sessiontitel, HTTP-Auth-Casing/kurzes Basic, gefälschte
  innere MCP-Fences); alle sechs geschlossen und erneut zur Gegenprüfung
  gegeben.
- Gates: `lf_p1_03_` 34/34, Redactor 50/50, Secret-Scanner 10/10,
  Sanitizer-Familie 142/142, Operator-Source 1/1, Dream-Cloud-Prompt 1/1,
  generierte CLI-Doku 1/1.

## Wave W9 — Exact-Windows-CI contract correction ✅
- `d73227ca` forensisch statt als Aggregate ausgewertet: Windows-Nextest stoppte
  beim Limit nach exakt 20 Fehlern; macOS war nur stale Docgen, Linux verlor den
  Hosted Runner ohne bestätigten Testfehler.
- Echte Produktionsfehler: `FreedomConfig::update_at` kann entfernte typed Keys
  jetzt aus dem strukturtreuen Unknown-Field-Overlay löschen (Split-Secrets und
  unbekannte Erweiterungen bleiben erhalten); `RuntimeState::default()` schreibt
  Cron-Schema v1; Doctor prüft CLIP über `cache_health_at` statt bloß `exists()`.
- Ehrliche Fixtures/Assertions: Manifest-derived sparse Whisper/CLIP-Caches,
  echter v32→v33-Migrationszustand, kanonischer Windows-Pfad, Doctor-Registry 57,
  VectorClock mit authentifiziertem Origin, Unbounded-Cost vor Autonomy,
  fail-closed malformed Sidecars und vollständige anyhow-Ursachenketten.
- Öffentliche Doctor-Hilfe nennt keine veraltete harte Checkzahl mehr;
  `docs/cli-commands.md` ist über den offiziellen Generator aktualisiert.
- Gates: ursprüngliche + Sidecar-Verträge 25/25, CLIP-Health 3/3, Docgen 1/1,
  rustfmt/diff sauber; drei Cluster-Reviews + stabiler Integrationsreview PASS.
- `GOLD-R3-10` bleibt offen, bis der gepushte Correction-Head CI, Security und
  CodeQL exakt grün beendet.

## Wave W10 — Canonical session preview + cross-platform CI correction ⏳
- `GOLD-LF-P1-20` ist vollständig implementiert, bleibt aber bis zum neuen
  Exact-Head-Beweis offen: kanonische read-only `raw_turns`-Reader liefern
  Preview und Sessionhistorie ohne Migration oder erfundene Fallback-Texte;
  Storage bleibt byte-exakt, jede GUI-Ausgabe wird erneut sanitisiert,
  whitespace-normalisiert und bei 64 erweiterten Graphemclustern begrenzt.
- Der Live-Sidebarzustand aktualisiert bei Send, Completion, terminalem Fehler,
  Delete, Startup und Reload, nicht bei Stream-Chunks. Send-before-Probe kann
  nicht mehr von kalter Historie überschrieben werden. A→B→Live-Laderennen
  werden über Revision plus Session-ID verworfen; historische Sessions sind
  sichtbar read-only und können weder senden, retryen, löschen noch Modell oder
  Link-Chips mutieren.
- Der erste adversariale Review fand neun reale Blockergruppen: Format/Lints,
  Send-before-Probe-Race, unsichtbare letzte Rows, verschluckte SQLite-Fehler,
  Legacy-Titel-Egress, fehlender Race-Beweis, unvollständige Tastatur-/AT-Wire,
  kontrastschwache Parent-Opacity und eine veraltete Dependency-Aussage. Alle
  neun wurden ursächlich geschlossen. Der finale Re-Review fand danach noch
  vier konkrete Restlücken: Partial-Chunks bei spätem Probe, nur am Send-Button
  gesperrte History, die kollidierende Session-ID `live` und feste Zeilenhöhen.
  Die Preview ignoriert jetzt jede Streaming-Row; History unmountet den gesamten
  Composer und gattet auch alle Rust-/Drag-drop-Mutationen vor State-Änderung;
  ein separater Bool typisiert Live/History; Zeilen wachsen aus
  `preferred-height`. Derselbe adversariale Reviewer beendete den Folgelauf PASS.
- Exact Head `830f20c9` beendete Security und CodeQL grün, aber CI deckte drei
  Linux-Clippy-Warnungen, 20 Windows-Vertragsfehler und zwei zusätzliche
  macOS-Fehler auf. Die lokale Korrektur umfasst den echten `FallbackConfig`
  Default, aktuelle Catalog-Fixtures, sichere MCP-Labels, kanonische
  Hardware/STT-Fixtures, korrekte f64-Caps, isolierte Live-Delivery-WAL-Keys,
  einen plattformidentischen versteckten Updater-Clap-Baum sowie fail-closed
  Nicht-Windows-Ausführung. Drei unabhängige Reviews fanden keine Lockerung der
  Produktionsgrenzen; Windows-Runtime und macOS-Lane sind PASS.
- Lokale Gates: `cargo fmt --all -- --check`, `git diff --check`,
  `cargo metadata --no-deps` und direkte Slint-Codegenerierung grün. Vollständige
  native Tests erreichen auf diesem Host wegen fehlendem `cl.exe`/`ml64.exe`
  nicht den Projektcode. Deshalb weder P1-20 noch R3-10 vor dem gepushten
  Linux/macOS/Windows-Workflow als abgeschlossen markieren.
- Der erste gepushte W10-Head `dcb6b13c` bestätigte CodeQL, Gold-Smoke,
  Feature-Flags, Signer, Lizenz-/Installer-, Keet- und WASM-Verträge grün, deckte
  aber in Stable- und Beta-Clippy denselben test-only Compile-Fehler auf: zwei
  neue Hardware-Cache-Helfer gaben `PathBuf` zurück, ohne den Typ im Testmodul
  zu importieren. Commit `79bacc6b` ergänzt ausschließlich diesen Import;
  Format und Diff sind grün. Ein neuer Exact-Head-Lauf bleibt erforderlich.
