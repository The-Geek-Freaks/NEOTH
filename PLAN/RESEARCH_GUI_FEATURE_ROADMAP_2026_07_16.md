# NEOTH GUI — Deep Feature-Gap Roadmap (2026-07-16b, workflow wf_45aad5af-8e3)

> 5-Agenten Deep-Audit (kanban/cluster/right-click/best-in-class[Claude·ChatGPT·Codex·Composer]/CLI-parity), je mit `current_state` aus echtem Code + `build_approach`. 57 findings, 684k tokens.
> **Befund: fast alles needs-rust + Codex-Zone** (main.slint/main.rs/settings.slint/panel_logic.rs) ODER needs-live-verify. Right-click-Architektur = ContextMenu in main.slint (3 schon gemountet: regen/kanban/buddy) → JEDES neue Kontextmenü ist Codex-Zone. **Handoff für Codex** (Rust/main.slint-Lane); Claude-Lane = nur `pure-slint`+non-codex.
> ⚠ **BUG gefunden:** cluster peer-expand Handler un-wired in main.rs (mesh.slint:369 `peer-expanded` feuert, kein Rust-Handler) — silent no-op. Für Codex/Tracker.


## HIGH · needs-live-verify · CODEX-ZONE

- [ ] **artifact-canvas-side-panel** `chat.slint`
  - now: H19-lite code-block panel (line 260-402) renders code inside the bubble — mono Text in a collapsible Rectangle with Copy chip and chevron collapse. ChatView (line 846) has no artifact-panel-open, no right-panel Rectangle
  - gap: Claude Desktop artifact panel, ChatGPT Canvas: a resizable side-panel slides out to the right of the scrollback when a reply contains significant code or long-form content. The AI writes into the panel; operator can high
  - build: Add `artifact-panel-open: bool`, `artifact-content: string`, `artifact-kind: string` ('code'|'markdown') to ChatView (chat.slint:846). In the main HorizontalLayout (line 941) add a right-panel Rectangle that animates width 0px→45% of parent via `animate width { duration: Theme.duration-entrance; }` controlled by artifact-panel-open. In ma
- [ ] **No drag-between-columns — cards move only via context menu or detail-pane buttons** `settings.slint:339`
  - now: KanbanColumn (settings.slint:339-391) renders cards via a for-loop of KanbanTaskCard. KanbanTaskCard (settings.slint:245-332) has a TouchArea for click and right-click only — no moved callback, no drag tracking. Column m
  - gap: Every best-in-class kanban tool (Linear, Height, GitHub Projects, Jira, Trello) provides drag-and-drop as the primary interaction for moving tasks between columns. The current approach requires two clicks (right-click → 
  - build: Slint has no built-in drag-drop across sibling components. Approach: add TouchArea.moved + pressed callbacks inside KanbanTaskCard to track an in-flight drag offset (position delta from press origin). Emit a new drag-started(task-id) callback upward to KanbanColumn, which emits it further to the board's HorizontalLayout in settings.slint.

## HIGH · needs-rust · CODEX-ZONE

- [x] **Peer expand/collapse Rust handler — completely un-wired (silent bug)** `main.rs` — ✅ STALE/ALREADY-RESOLVED (verify-first 2026-07-17): the toggle now lives in Slint at main.slint:4001 (`peer-expanded(id) => { mesh-expanded-peer = (mesh-expanded-peer == id) ? "" : id; }`), an `in-out` prop forwarded to MeshView (main.slint:3994) → PeerCard `expanded` (mesh.slint:688) → detail block (mesh.slint:298). Fully functional in pure Slint; NO Rust handler needed (correct arch — same as the "ContextMenu in main.slint" rule). Roadmap was written before that handler landed.
  - now: mesh.slint:364 defines `in property <string> expanded-peer: ""`, mesh.slint:369 defines `callback peer-expanded(string)`, and mesh.slint:683-684 drives `expanded: root.expanded-peer == peer.id` plus fires `root.peer-expa
  - gap: Clicking 'Details ›' fires the callback to Rust but Rust never calls `w.set_expanded_peer(...)`, so the `expanded` boolean on every PeerCard stays false forever. The expand button is visually present but does nothing. Se
  - build: Add a thread-local `Arc<Mutex<String>>` named `expanded_peer_id` initialized to empty. Register `window.on_peer_expanded(move |peer_id| { ... })` immediately after the `on_mesh_peer_sync_clicked` block at main.rs:5866. Inside the handler: toggle logic — if `expanded_peer_id.lock() == peer_id` set it to `""` (collapse), else set it to `pee
- [x] **Auto-refresh / live polling loop absent** `main.rs` — ✅ DONE 2026-07-17: dedicated 30s polling thread after the startup one-shot (main.rs ~6054), gated on an `Arc<AtomicBool>` toggled from the GUI, self-terminates via `invoke_from_event_loop(||{}).is_err()` when the window closes. Operator pauses via the "⏸ Auto"/"▶ Auto" NeothButton in the mesh header (mesh.slint) — `in-out mesh-auto-refresh-enabled` + `mesh-auto-refresh-toggled(bool)` callback forwarded through main.slint to `on_mesh_auto_refresh_toggled`. GUI gate green.
  - now: `refresh_mesh` is called exactly twice: once at startup (line 5840, one-shot thread) and once on manual Refresh button click (line 5833). There is no periodic background loop. The mesh panel silently goes stale indefinit
  - gap: Grafana, Netdata, and Tailscale admin all auto-refresh every 5–30 seconds. A cluster dashboard that requires manual interaction to detect a peer going offline is useless for incident response. The peer `staleness_secs` c
  - build: After the `weak_mesh_init` one-shot thread at main.rs:5839, spawn a second dedicated thread that loops: `loop { std::thread::sleep(Duration::from_secs(30)); refresh_mesh(weak_poll.clone()); }`. Add `in property <bool> mesh-auto-refresh-enabled: true;` and a toggle NeothButton to MeshView so the operator can pause polling. Gate the loop on
- [ ] **Role chip and version badge always empty — never populated from any data source** `main.rs`
  - now: main.rs:14631 has `role: "".into(), // roles land with the gossip payload extension` and main.rs:14632 has `version: "".into(), // ditto — neoth_version is not gossiped yet`. The MeshPeerRow fields exist in the Slint str
  - gap: k9s and Tailscale admin always display node role (worker/coordinator/relay/exit-node) and daemon version per node. An operator managing a 3-node cluster cannot tell which node is the coordinator or whether a peer is runn
  - build: Two parallel tracks: (1) Add `pub role: String` and `pub neoth_version: String` to `NodeResourceSnapshot` in swarm.rs, populated from the `cluster_roles` SQLite table and the binary version constant (`crate::VERSION`). These piggyback on the existing 30s gossip heartbeat snapshot. (2) In the meantime, add `role` and `version` keys to the 
- [ ] **agent-step-timeline-in-chat-thread** `agents_view.slint`
  - now: AgentsView (agents_view.slint:265) is a standalone 'Agents' nav tab — card grid refreshed by polling, not reactive to a specific chat message. LoopPanel (loop.slint:1) is a separate 'Loops' tab. gui_stream.rs (line 43-45
  - gap: Codex in ChatGPT Desktop: when the agent works on a message, a collapsible step-tree expands below the triggering user bubble inline in chat — 'Step 1: Read file ✓ / Step 2: Write patch... running / Step 3: Run tests pen
  - build: Add `AgentStep { label: string, state: string, elapsed: string, output: string }` struct (export from agents_view.slint) and `steps: [AgentStep]` field to ChatMessage (chat.slint:57). Add a `role: 'agent-progress'` arm in ChatBubble (line 147) that renders a VerticalLayout of step rows: Led component (state->led mapping mirrors AgentCard:
- [ ] **Chat ConversationItem: right-click channel with Mark Read / Remove Channel shortcut** `chat.slint`
  - now: ConversationItem (chat.slint:518) has only `callback clicked()` (line 527). Single-click switches to the channel. No pointer-event, no context-menu. The only path to remove a channel from the GUI is the 4-step detour: na
  - gap: No right-click. Removing a channel from the conversation sidebar requires 4 clicks through Settings. Right-click on the sidebar item is the industry-standard UX (Slack, Discord, Telegram all have it). Actions: Mark as re
  - build: Add `pointer-event` to ConversationItem's TouchArea (chat.slint:527 area), detect right-up, fire `callback context-menu(string, length, length)` with the channel label. ChatView (chat.slint) re-emits as `callback channel-ctx(string, length, length)`. AppWindow gains `channel-ctx-open/name/x/y` + `ContextMenu` [Mark read · Clear history · 
- [ ] **message-branch-navigation** `chat.slint`
  - now: ChatMessage struct (line 57-80) has role/text/timestamp/streaming/metrics/code-block — no branch_id, branch_count, branch_index fields. messages model is a flat `in property <[ChatMessage]> messages: []` (line 847). mess
  - gap: Claude Desktop and ChatGPT Desktop edit-and-branch: click a prior user bubble, edit text, re-send → creates branch B; ← → chevrons navigate A↔B. NEOTH's retry is a re-send that overwrites the tail, destroying the origina
  - build: Add `branch-id: string, branch-index: int, branch-count: int` to ChatMessage (chat.slint:57). In main.rs switch the message store from Vec<ChatMessage> to HashMap<String,Vec<ChatMessage>> keyed by branch_id with a current_branch pointer. message-retry(idx) forks: copy messages[0..=last_operator_before_idx] into a new branch key, re-issue 
- [ ] **inline-tool-approval-cards** `chat.slint`
  - now: chat.slint:1096-1258 consent strip toggles operating-mode (gated/full-auto) at session level and shows per-provider revoke popup — mode-level control only, not per-tool-call. ChatView callbacks block (line 881-910) has n
  - gap: Codex in ChatGPT Desktop, Cursor: when the agent needs to execute a tool call (file write, shell command, web fetch) during a chat session, an inline approval card appears in the scrollback showing tool name, args, risk 
  - build: Add `tool-name: string, tool-args: string, tool-risk: string` fields to ChatMessage (chat.slint:57) and a `role: 'tool-approval'` rendering arm in ChatBubble (line 147): a Card with tool-name header, mono args block, risk chip, and [Approve]/[Edit Args]/[Deny] NeothButtons. Add `approve-tool(int)` + `deny-tool(int)` + `edit-tool-args(int,
- [ ] **at-mention-context-picker** `chat.slint`
  - now: chat.slint:716-731: slash-active badge (a '/' Rectangle) appears when draft starts with '/' — the pattern exists for symbol-triggered UI. chat.slint:668-698: 📎 attach button fires attach-clicked() for native OS file-pick
  - gap: Cursor Composer @file/@folder/@docs/@web/@codebase; Claude Desktop @-file attachment with type selection. Typing '@' opens a picker with context types that resolve to injected context (file content, memory node text, Obs
  - build: Add `at-active: bool`, `at-query: string`, `at-suggestions: [string]` to Composer (chat.slint:609). Mirror the slash-active dropdown pattern (line 716): when at-active=true, render a PopupWindow below the TextInput listing at-suggestions as clickable chips. In main.rs on `composer-draft-edited(text)`, detect '@' trigger: '@memory' queries
- [x] **Architecture: ContextMenu overlay must live in main.slint, never self-contained in a sub-component** `main.slint` — ✅ STALE (verify 2026-07-17): 3 production ContextMenus already in main.slint (regen ~4068, kanban ~4083, buddy ~4120). Pattern established.
  - now: Three ContextMenu instances already mounted in main.slint at z-top: regen-menu (line 3987), kanban-ctx (line 4002), buddy-menu (line 4039). All follow the same pattern: `in-out property <bool> xxx-ctx-open` + `in propert
  - gap: ContextMenu CANNOT be self-contained inside a sub-component file. It needs width:100%; height:100% relative to the AppWindow root, because layout-nested components are clipped to their panel bounds. Sub-views fire `callb
  - build: Rollout pattern for every new context target: (1) Add pointer-event handler inside component's TouchArea detecting PointerEventButton.right + PointerEventKind.up, compute absolute-position + mouse-x/y, fire callback context-menu(id, x, y). (2) View layer re-emits as `xxx-ctx(id, x, y)` callback upward. (3) main.slint AppWindow gains `in-o
- [ ] **Memory graph nodes: right-click with Show Detail / Forget / Export** `memory_graph.slint`
  - now: `node-ta := TouchArea` at memory_graph.slint:385 fires `root.node-selected(node.id)` on click and accessible-action-default. MemoryGraphView exposes `callback node-selected(int)`, `callback export-clicked()`, `callback r
  - gap: No right-click on graph nodes. Forgetting a specific memory (neoth memory forget --id) is only accessible via CLI — there is no GUI path at all. Right-click is the natural UX for per-node destructive actions: Show detail
  - build: Add `pointer-event` to `node-ta` in memory_graph.slint:385 alongside existing `clicked`, detect right-up, fire `callback node-context(int, length, length)` on MemoryGraphView. MemoryGraphView re-emits as `callback memory-node-ctx(int, length, length)`. AppWindow gains `memory-node-ctx-open/id/x/y` + `ContextMenu` [Show detail · Forget nod
- [ ] **Kick / revoke / promote-role peer actions absent from GUI** `mesh.slint`
  - now: PeerCard quick-actions (mesh.slint:252-293) has exactly two buttons: `NeothButton { label: "Request Sync"; }` and a ghost 'Details ›' toggle. The expanded detail block (mesh.slint:296-334) shows only read-only text rows 
  - gap: k9s has per-node delete/cordon/drain. Tailscale admin has 'Remove device', 'Disable exit node', 'Change device owner'. Real cluster management requires control actions — the GUI currently only lets operators observe, not
  - build: In mesh.slint PeerCard, add `callback kick-clicked()` and `callback promote-clicked()` properties at the same level as `sync-clicked` (line 74). Inside the expanded detail block (line 296), add an `HorizontalLayout` with `NeothButton { label: "Revoke Peer"; size: "sm"; }` (consent-colored) and `NeothButton { label: "Promote Coord"; size: 
- [ ] **Topology graph / visual network map absent** `mesh.slint`
  - now: The PEERS card (mesh.slint:641) renders peers as a flat vertical stack of `PeerCard` components with no spatial layout, no edges between nodes, no RTT-labeled connections, no cluster shape. Slint 1.16 has no native Canva
  - gap: Tailscale admin shows a force-directed graph with peer nodes and RTT-labeled edges. k9s shows node relationships. A linear list of 10 peers gives no sense of cluster topology — which nodes are well-connected, which are r
  - build: Generate a compact SVG topology string from Rust in `refresh_mesh`: nodes as labeled circles positioned in a circle layout, edges as lines between reachable peers, edge labels with RTT (once RTT is available) or staleness. Add `in property <image> mesh-topology-svg: @image-url("");` to MeshView and set it via `slint::Image::from_svg_data(
- [ ] **Health heatmap (time × peer) absent — instantaneous snapshot only** `mesh.slint`
  - now: `MeshPeerRow` (mesh.slint:29-39) has `reachable: bool` and `staleness-secs: int` — both are instantaneous current-state values. There is no historical availability tracking at any layer. `SwarmTable` (swarm.rs:161) store
  - gap: Grafana and Netdata show a 24h time × peer grid (heatmap) so operators can see that node-B was flaky every night between 02:00–03:00. Without history, NEOTH cannot distinguish a momentarily-offline peer from a chronicall
  - build: Add `pub health_history: VecDeque<(i64 /*ts_unix*/, bool /*healthy*/)>` (capped at 288 samples = 24h at 5min buckets) to `SwarmTable` per node. The heartbeat tick appends a sample on every gossip receive. Expose the last 24 entries as a `[HealthSample]` model (`in property <[bool]> health-buckets`). In mesh.slint add a `for h in peer.heal
- [ ] **Mesh peer cards: right-click with Force Sync / Copy ID / Remove Peer** `mesh.slint`
  - now: PeerCard (mesh.slint:62) has `callback sync-clicked()` (line 74) and `callback expand-clicked()` (line 75), wired in MeshView as `peer-sync-clicked(string)` and `peer-expanded(string)` (lines 684-685). No pointer-event, 
  - gap: No right-click. The Remove Peer action is completely absent from the GUI — the only path to remove a mesh peer is the CLI. `neoth cluster remove` (cluster.rs:213-216) takes a pub_key or unique prefix and hot-reloads the 
  - build: Add `pointer-event` to PeerCard's expand-clicked TouchArea area (or wrap body in a new TouchArea), fire `callback context-menu(string, length, length)` with peer-id and absolute coords. MeshView re-emits as `callback mesh-peer-ctx(string, length, length)`. AppWindow gains `mesh-peer-ctx-open/id/x/y` + `if mesh-peer-ctx-open: ContextMenu` 
- [ ] **multi-file-diff-review-pane** `selfreprog.slint`
  - now: SelfReprogProposalRow (selfreprog.slint:24-35) has `patch-path: string, diff-sha256: string, is-source-edit: bool` — the data pointers exist. apply-confirm popup (line 66-80) renders a plain confirmation Rectangle with n
  - gap: Cursor Composer multi-file diff pane: old vs new columns with syntax-highlighted changed lines, per-hunk [Accept]/[Reject] chips, summary header ('3 files changed, 47 insertions, 12 deletions'). NEOTH has the patch path 
  - build: Create `diff_view.slint` with `DiffHunk { file: string, hunk-header: string, old-lines: [string], new-lines: [string], accepted: bool }` and a DiffView component (ScrollView over colored line rows: deleted=Theme.consent.with-alpha(0.15), inserted=Theme.memory.with-alpha(0.15), unchanged=transparent, all mono font). In selfreprog.slint, re
- [x] **Operator notes GUI is single-line only — no multi-line textarea on or near card** `settings.slint:4275` — ✅ DONE 2026-07-17: new NeothTextArea component (components.slint, mirrors NeothLineEdit tokens/placeholder/focus-ring, multi-line word-wrap TextInput); comment composer (settings.slint) swapped LineEdit→NeothTextArea. Enter=newline, Add button submits. Callback kanban-task-comment(string,string) unchanged → no Rust. GUI gate green.
  - now: The Rust injection path IS shipped (dispatcher.rs:394-403, append_task_notes). The GUI side is a single-line NeothLineEdit (wrapping std-widgets LineEdit) in the detail pane (settings.slint:4274-4283). It fires kanban-ta
  - gap: Can only write one-line notes; multi-paragraph steering instructions (e.g. 'use the existing auth module, avoid modifying X, apply pattern Y') cannot be entered. The input is not visible until the operator explicitly cli
  - build: Add a multi-line NeothTextArea component in components.slint wrapping Slint std-widgets TextEdit. In the detail pane section of settings.slint (around line 4274), replace the NeothLineEdit with NeothTextArea and wire its edited/accepted callbacks to kanban-task-comment. In main.rs, update the kanban-task-comment handler to accept multi-li
- [ ] **No inline patch diff viewer in the detail pane** `settings.slint:4060`
  - now: patch_path is stored in idx_kanban_task (store.rs:81) and patch_path: Option<PathBuf> exists on KanbanTask (types.rs:270). The detail pane (settings.slint around line 4060) renders title, status, hemisphere, and kanban-s
  - gap: The operator cannot see what the worker actually produced without leaving the GUI and running 'neoth kanban task <id>' in the CLI. Linear and GitHub Projects both show the artifact (diff / PR / commit) directly in the ca
  - build: Add 'in property <string> kanban-selected-patch: ""' to SettingsView (settings.slint) and propagate to main.slint AppWindow. In main.rs, inside the kanban-task-selected callback, read the patch_path from the task row, open the file, and push the text to kanban-selected-patch (cap at ~64 KB to stay inside Slint's string limit). In settings
- [ ] **Credential management panel (list / import / scan)** `credential.rs`
  - now: Credentials are written once during the wizard (write_credentials_yaml in main.rs) and never surfaced again post-onboarding. CLI credential.rs has: list (key-names only, never values), import --file <path> (merge YAML), 
  - gap: Operators have no way to confirm which API keys are set, update a rotated key by importing a new credentials.yaml, or scan a repository for accidentally committed secrets — all common day-2 operations.
  - build: Add a 'credentials' NavBtn in app_shell.slint (complexity >= 1). Create credential.slint with: (1) a CredKeyRow {name, is_set} list from neoth credential list --output json, auto-loading on open; (2) an Import section with a file-path LineEdit and Import button wired to on_credential_import_clicked(path) -> neoth credential import --file 
- [ ] **Groundtruth write actions (add / revoke / state / contradictions)** `groundtruth.rs`
  - now: main.slint:3612 has a read-only ProbeView for neoth groundtruth list (string output only). CLI groundtruth.rs has add (statement + scope), revoke (id), state (id, raw|candidate|verified|superseded|contradicted|deprecated
  - gap: Operators can read the fact store from the GUI but cannot add a verified fact, revoke a stale one, promote a candidate to verified, or dismiss contradiction pairs — the daily data-quality operations that keep recall accu
  - build: Replace the ProbeView string display at main.slint:3612 with a new GroundtruthView component (groundtruth.slint) that renders typed GroundtruthRow {id, statement, scope, state, ts} from neoth groundtruth list --output json. Add an Add-fact form (statement LineEdit + scope selector: global/host/session) and per-row action buttons: Revoke (
- [ ] **Model management panel (pull / prune / recommend)** `models.rs`
  - now: ResourcesView in tabs.slint:87 shows a static models-count StatRow. main.rs:1290 calls neoth models catalog internally for the wizard model picker. main.rs:12304 uses neoth models recommend internally for the settings mo
  - gap: There is no panel where an operator can see which local models (CLIP, Whisper, Qwen) are downloaded, pull new ones, prune cache to free disk, or get a hardware-appropriate model recommendation for their VRAM tier.
  - build: Add a 'models' NavBtn under INTELLIGENCE (complexity >= 2). Create models.slint with a ModelRow {name, status: cached|missing, size_bytes} list from neoth models list --output json, with Pull and Prune action buttons per row wired to on_model_pull_clicked(name) and on_model_prune_clicked(name) in main.rs. Add a Recommend section below: sh
- [ ] **Moral Core editor** `moral_core.rs`
  - now: CLI has full CRUD: list (JSON: tag/directive-count/source-file), preview (injected text), doctor, init, add, remove, enable, disable, template {list,show,add}. ZERO presence in any GUI file — no NavBtn in app_shell.slint
  - gap: No GUI panel exists for viewing, adding, removing, enabling, or disabling the directives that gate every response. Operators must leave the GUI and use the CLI to manage the most powerful single config surface in NEOTH.
  - build: Add a 'moralcore' NavBtn in app_shell.slint under the INTELLIGENCE group (complexity >= 1). Create moral_core.slint with three zones: (1) a read-only ProbeView-style block calling neoth moral-core preview --output json auto-loading on open; (2) a typed list of MoralCoreRow {category, directive_count, enabled, path} from neoth moral-core l
- [ ] **Rollback apply (execute mutation reversal)** `rollback.rs`
  - now: settings.slint:4917 has a 'Preview rollback' button. main.rs:8744 shells neoth rollback list (read-only, --output json) and shows output in status-line. rollback.rs has apply --to <offset> --segment <path> [--confirm] wh
  - gap: Operators can see available WAL snapshots but cannot restore one from the GUI. The canonical recovery flow (neoth rollback list -> pick offset + segment -> neoth rollback apply --confirm) has no GUI path.
  - build: In settings.slint below the existing rollback-preview section (line ~4917), add a structured apply form: offset LineEdit, segment-path LineEdit, a 'I understand this overwrites the current file' NeothCheckBox, and a 'Apply snapshot' NeothButton gated on checkbox. Wire settings.slint callback rollback-apply-clicked(offset, segment) -> main
- [ ] **RTT / network latency per peer — no measurement anywhere** `heartbeat.rs`
  - now: `HeartbeatBody` (heartbeat.rs:218) carries `tokens_per_sec: f64`, `inflight_requests: u32`, `healthy: bool`, `capabilities_hash: [u8; 32]`. `WireFrame` (heartbeat.rs, referenced in swarm.rs) carries `sent_unix_ms` but th
  - gap: Tailscale admin shows per-peer RTT with ms precision. Netdata shows network latency graphs. Without RTT data, NEOTH cannot distinguish a 2ms LAN peer from a 400ms WAN peer for routing decisions. The `LeastLoaded` policy 
  - build: Add a `Pong` variant to `FrameKind` (heartbeat.rs:119). On receiving a heartbeat, the receiver immediately sends a Pong carrying back the sender's `sent_unix_ms`. The original sender computes `rtt_ms = now_unix_ms() - sent_unix_ms` and records it in a per-peer RTT table in `SwarmTable` (swarm.rs). Expose `rtt_ms: Option<u32>` in the `clus
- [ ] **No priority or label columns on task data model or card face** `store.rs:67`
  - now: idx_kanban_task schema (store.rs:67-90) has no priority or labels column. KanbanTask struct (types.rs:255-272) has no priority or labels field. KanbanTaskRow (settings.slint:30-34) has only {task-id, title, hemisphere}. 
  - gap: Without priority, the dispatcher picks tasks by insertion order only (list_backlog_tasks_for_session returns in rowid order per the SQL). Operators cannot mark a task urgent or flag technical debt vs feature work. Labels
  - build: Add 'priority TEXT NOT NULL DEFAULT "med" CHECK(priority IN ("p0","p1","med","low"))' and 'labels TEXT NOT NULL DEFAULT "[]"' to the schema in store.rs SCHEMA_SQL (store.rs:50-122). Add priority: String and labels: Vec<String> to KanbanTask (types.rs) and priority: string to KanbanTaskRow (settings.slint:30-34). In KanbanTaskCard (setting

## MED · pure-slint · ✅CLAUDE-SOLO

- [ ] **Gossip event stream: no severity coloring, no peer filter, no pause** `mesh.slint`
  - now: The GOSSIP card (mesh.slint:727-793) renders up to 60 lines (`format_gossip_lines(&foreign_out, 60)` at main.rs:14607) with all lines in a single `Theme.text-tertiary` color (mesh.slint:785). There is no way to filter by
  - gap: Netdata's event log has severity badges (info/warn/error) with distinct colors and a per-source filter. The raw foreign events in `idx_foreign_events` have `event_type` bytes that map to severity (e.g., 0x90/0x91 = memor
  - build: Severity coloring is pure-slint and requires no Rust change: replace the single `Text` at mesh.slint:783 with a conditional color expression. Parse a severity prefix from the line string: if `evt.starts-with("ERR") || evt.starts-with("0x9")` → `Theme.consent`, `evt.starts-with("WARN")` → `Theme.warning`, else `Theme.text-tertiary`. For pe
  - ⚠ CORRECTION (2026-07-17, investigated): this item is NOT pure-slint and is partially BLOCKED. (1) COLORING: this Slint build cannot call `.starts-with()`/string methods in property expressions (see chat.slint:860 — the host owns such booleans), so the string-prefix approach is impossible. It needs Rust: `format_gossip_lines` (panel_logic.rs:3166) must emit a parallel `[int] gossip-severities` (int comparison IS allowed in slint) derived from the `event_type` byte — BUT there is NO opcode→severity classifier in the codebase (grepped wal/*.rs), so the byte→{info,warn,error} map must be DEFINED first (a real decision, not a guess; guessed severities are worse than uniform color). (2) PAUSE: pure-slint (private `[string] paused-snapshot` + `for evt in (pause-active ? paused-snapshot : gossip-events)`, snapshot-copy in the toggle callback) — but marginal until the auto-refresh/polling item (HIGH·needs-rust) lands, since the stream only changes on manual refresh. (3) PEER FILTER: needs Rust model-filtering (Slint can't filter a `[string]` model inline in a `for`); the peer is available as `origin_peer_pk`/peer8 in panel_logic.rs:3176. → Reclassify: **needs-rust + needs a severity-map decision**, not CLAUDE-SOLO-pure-slint.

## MED · pure-slint · CODEX-ZONE

- [x] **No WIP limit visualization or enforcement on columns** `settings.slint:334` — ✅ DONE 2026-07-17: KanbanColumn gained `wip-limit` (settings.slint:342); count shows N/limit + turns Theme.warning and the column border warns when tasks.length>=limit (`over-wip`). Board defaults IN-PROGRESS:3, REVIEW:5. Pure-slint, GUI gate green.
  - now: KanbanColumn (settings.slint:339-391) shows the task count as 'Text { text: root.tasks.length; }' (line 369) with a 2px accent underline. There is no wip-limit property, no color change when the column is full, and no en
  - gap: WIP limits are the core discipline mechanism of kanban (Lean, Kanban Method). Without them the In-Progress column can accumulate unbounded tasks, which defeats the point of the board and makes the dispatcher's concurrent
  - build: Add 'in property <int> wip-limit: 0' to KanbanColumn (settings.slint:339). Change the count Text color to Theme.warning when wip-limit > 0 && root.tasks.length >= root.wip-limit, else Theme.text-muted. Add a thin amber/red top border on the column when over-limit. Wire sensible defaults in the board (BACKLOG: 0=unlimited, IN-PROGRESS: 3, 

## MED · needs-live-verify · CODEX-ZONE

- [ ] **Detail pane opens below the board disrupting the layout — no non-disruptive peek panel** `settings.slint:4060`
  - now: Clicking a task card sets kanban-selected-task-id which makes the detail pane visible as a VerticalBox stacked below the board HorizontalLayout (inferred from the if-guarded VerticalBox at settings.slint:4060). This push
  - gap: Linear, Height, and GitHub Projects all use a right-side slide-in peek panel that preserves the board visible on the left. The panel slides in at ~35% width, the board stays at 65%, and the operator can close the peek to
  - build: Restructure the Code Sessions content (settings.slint:3818-4300) from a VerticalBox to a HorizontalLayout where the left pane is the board (horizontal-stretch: 2) and the right pane is the peek panel (width animates 0 <-> 400px when kanban-selected-task-id is non-empty, using Theme.duration-entrance cubic-bezier). Move the detail pane con

## MED · needs-rust · CODEX-ZONE

- [ ] **'Request Sync' fires a read probe, not an actual WAL sync trigger** `main.rs`
  - now: `on_mesh_peer_sync_clicked` (main.rs:5846-5866) runs `neothd cluster sync-state --peer X --output json`, truncates stdout to 160 chars, and pushes a toast. It does NOT enqueue any WAL frame or wake the gossip tick. The c
  - gap: The gossip tick runs every 30 seconds (wal_sync.rs:45 `GOSSIP_TICK_INTERVAL`). An operator who clicks 'Request Sync' expecting to push pending frames immediately gets a state report instead. There is no way to trigger an
  - build: Add a CLI subcommand `neothd cluster sync-now --peer <pub_key>` that signals the running daemon (via a sidecar JSON file in the spirit of `audit_sidecar.rs`) to immediately run one gossip tick for that peer. In main.rs change `on_mesh_peer_sync_clicked` to call this command and show 'Sync queued' on success. Keep the current read-probe as
- [ ] **WAL vector-clock delta per peer shown only as raw truncated toast** `main.rs`
  - now: main.rs:5858 does `let summary: String = out.trim().chars().take(160).collect()` on the raw JSON from `cluster sync-state --peer X`. `GossipState::last_seen_seq` (wal_sync.rs:374) returns `Option<u64>` per peer and the d
  - gap: Netdata shows per-peer replication lag as a named metric ('X events behind, last synced N seconds ago'). The data exists in the daemon but is buried in a 160-char toast that an operator cannot act on. The expanded PeerCa
  - build: Add `sync-lag-events: int` and `sync-cursor-age: string` to `MeshPeerRow` in mesh.slint:29. In `refresh_mesh` at main.rs:14615, for each peer call `run_neothd_probe(&["cluster", "sync-state", "--peer", &p.id, "--output", "json"])` and parse `lag_events` and `cursor_ts` from the JSON. Set these on each MeshPeerRow. In the PeerCard expanded
- [ ] **Chat session history rows: right-click with Export / Delete / View in WAL** `chat.slint`
  - now: `sess-touch := TouchArea { clicked => { root.session-selected(s.id); } }` at chat.slint:1044. The session history sidebar (lines 1007-1074) renders past sessions as Rectangles with only a selection callback. No export, n
  - gap: No interaction beyond selection. Export session transcript and Delete session are both absent from the GUI entirely. These map to `neoth memory list-sessions` (memory.rs) and WAL export with a session filter. Right-click
  - build: Add `pointer-event` to `sess-touch` in chat.slint:1044 alongside `clicked`, detect right-up, fire `callback session-context(string, length, length)`. ChatView re-emits as `callback session-ctx(string, length, length)`. AppWindow gains `session-ctx-open/id/x/y` + `ContextMenu` [View in WAL · Export session · Copy session ID] in main.slint.
- [ ] **Chat message bubbles: right-click as alternative to hover action chips** `chat.slint`
  - now: ChatBubble (chat.slint:116) has `copy-clicked()`, `retry-clicked()`, `delete-clicked()` (lines 123-125) exposed via hover chips that appear when `bubble-hover.has-hover` is true. `bubble-hover := TouchArea {}` at line 16
  - gap: No right-click. Hover chips are the only action surface; they disappear when mouse leaves the bubble and are sometimes hard to hit on fast-moving conversations. Right-click provides a reliable alternative activation path
  - build: Add `pointer-event` to `bubble-hover` in ChatBubble (chat.slint:167), detect right-up, fire `callback context-menu(int, length, length)` (bubble-index, x, y). ChatView for-loop re-emits as `callback bubble-ctx(int, length, length)`. AppWindow gains `bubble-ctx-open/idx/x/y` + `ContextMenu` [Copy · Retry · Delete] in main.slint, dispatchin
- [ ] **mid-chat-model-picker** `chat.slint`
  - now: Composer (chat.slint:609-841) has: attach 📎, slash badge, multi-line TextInput, Send/Stop. No model selector widget. ChatMessage.model (line 77) shows what provider responded post-turn. Model settings live in Settings → 
  - gap: Claude Desktop model dropdown in the chat header; ChatGPT model selector above composer. Switch model mid-session without leaving chat. 'Regenerate with...' on retry to compare outputs across models inline. NEOTH's model
  - build: Add `current-model: string`, `model-options: [string]` to ChatView (chat.slint:846) and `model-changed(string)` callback. Add a compact model chip inside Composer's body VerticalLayout (before the 'Message' label at line 657): a Rectangle (border-radius:Theme.radius-pill, background:Theme.surface-2) showing current-model, clicking shows a
- [ ] **inline-image-input-and-rendering** `chat.slint`
  - now: ChatMessage struct (line 57-80): role, text, timestamp, streaming, metrics, metrics-detail, model, code-block, code-lang — no image-url, image-data, or image-thumb field. pending-attachments strip (line 668-698) renders 
  - gap: Claude Desktop and ChatGPT Desktop: paste or drag an image into the composer → thumbnail preview in the attachment strip; the image is sent as a multipart content block. AI responses can include image URLs that render as
  - build: Add `image-url: string` and `image-thumb: image` to ChatMessage (chat.slint:57). In ChatBubble content VerticalLayout (after the code-block panel at line 402) add `if root.message.image-url != '': Image { source: root.message.image-thumb; max-height: 320px; image-fit: contain; }`. Add `pending-thumbs: [image]` to Composer (line 617) and r
- [ ] **Cron job cards: right-click to consolidate Run / Toggle / Edit / Remove into menu** `cron.slint`
  - now: CronJobCard (cron.slint:37) has three inline NeothButton rows: Run (line 119), Enable/Disable toggle (line 125), Remove (line 131). All actions exist as inline buttons in the card body. No pointer-event, no context-menu 
  - gap: No right-click menu. The inline button row is the only action surface. A right-click menu is additive (not replacing buttons, but providing a shortcut) and opens the door for an Edit action (edit cron schedule, prompt, c
  - build: Add an outer TouchArea to CronJobCard body (or the card-level rectangle) with `pointer-event` firing `callback context-menu(string, length, length)`. CronView re-emits as `callback cron-ctx(string, length, length)`. AppWindow gains `cron-ctx-open/id/x/y` + `ContextMenu` [Run now · Enable/Disable · Edit schedule · Remove] in main.slint. `c
- [ ] **Composite cluster health bar / aggregate status absent** `mesh.slint`
  - now: The PEERS card header (mesh.slint:648-670) shows a count chip with `mesh-peers.length` in cyan. Individual health rails exist per PeerCard. There is no aggregated 'N/M healthy' indicator, no cluster-wide health percentag
  - gap: k9s shows a resource summary bar at the top. Grafana has a service-level 'all green / N degraded' tile visible without scrolling. When there are 8 peers, an operator needs to scroll through all PeerCards to discover one 
  - build: Add `in property <int> mesh-healthy-peer-count: 0;` to MeshView. In `refresh_mesh` (main.rs:14615), compute `healthy_count = peer_rows.iter().filter(|p| p.reachable).count()` and call `w.set_mesh_healthy_peer_count(healthy_count as i32)`. In the PEERS card header (mesh.slint:648), replace the single count chip with two side-by-side chips:
- [ ] **No swimlanes — board is flat, no grouping by hemisphere or task-type within columns** `settings.slint:3929`
  - now: The board (settings.slint:3929-3981) is a flat HorizontalLayout of five KanbanColumn instances. All tasks in the In-Progress column — Left-hemisphere fast tasks and Right-hemisphere deep tasks — appear in the same unsort
  - gap: NEOTH has a named Left/Right/Cerebellum hemisphere model, which is a natural swimlane axis: Left tasks are fast UI/CRUD work, Right tasks are deep architecture/review. Mixing them in one column makes it impossible to see
  - build: Split kanban-in-progress (and kanban-review) into hemisphere buckets in main.rs: kanban-in-progress-left, kanban-in-progress-right, kanban-in-progress-cerebellum. Add these as in-property arrays on SettingsView (settings.slint). In the board layout, replace the single KanbanColumn for IN-PROGRESS with a vertical stack of three labeled sub
- [ ] **No batch-select or bulk actions on cards** `settings.slint:245`
  - now: KanbanTaskCard (settings.slint:245) has 'in property <bool> selected: false' which is set by task-id equality against kanban-selected-task-id (a single string). There is no checkbox, no shift-click range select, no multi
  - gap: Archiving a completed session requires clicking each Done card individually. Moving a batch of Blocked tasks back to Backlog for retry requires repeating the context menu 20 times for a large session. GitHub Projects, Li
  - build: Add 'checked: bool' to KanbanTaskRow struct (settings.slint:30). Add a floating HorizontalLayout action bar below the board that appears when any(kanban-backlog.checked || kanban-todo.checked ...) using Slint's conditional 'if'. Add callbacks kanban-bulk-move([string], string) and kanban-bulk-archive([string]) to SettingsView. In KanbanTa
- [ ] **No saved filter views — board always shows all tasks in selected session** `settings.slint:3818`
  - now: The Code Sessions tab (settings.slint:3818) has a session selector (kanban-sessions dropdown) and a Refresh button. There is no filter bar, no hemisphere filter, no task-type filter, and no saved-view concept anywhere in
  - gap: A session with 20+ tasks decomposed by Cerebellum becomes visually overwhelming with no way to focus. Operators cannot answer 'show me only Blocked tasks' or 'show me only Right-hemisphere tasks in Review'. Linear's filt
  - build: Add a filter bar HorizontalLayout above the board (settings.slint:3929) with NeothComboBox controls for hemisphere (All/Left/Right/Cerebellum) and task-type (All/ui/feature/fix/refactor). Add 'kanban-filter-hemisphere: string' and 'kanban-filter-type: string' as in-out properties on SettingsView, firing a kanban-filter-changed callback wh
- [ ] **Skills list rows: right-click with Test Skill action (missing from inline buttons)** `settings.slint`
  - now: Each skill row (settings.slint:1984) renders a NeothCheckBox (enable/disable, line 2006) and an Uninstall button with inline confirm (lines 2024-2043). The `skills.rs` CLI has a `Test` variant that runs the RED/GREEN sce
  - gap: The Test skill action (neoth skills test <id>) has zero GUI exposure. It only runs via CLI. Right-click on a skill row is the cleanest slot for it without adding a third inline button per row. Menu: Enable · Disable · Te
  - build: Wrap each `!s.is-header` skill row HorizontalLayout in a Rectangle with `pointer-event`, fire `callback skill-context(string, length, length)` on SettingsView (settings.slint). SettingsView re-emits as `callback skill-ctx(string, length, length)` which AppWindow (`main.slint`) captures. AppWindow gains `skill-ctx-open/id/x/y` + `ContextMe
- [ ] **Plugins list rows: right-click with Reconsent action (missing from inline UI)** `settings.slint`
  - now: Each plugin row (settings.slint:2107) has NeothCheckBox (lines 2112-2116), optional Activity button (line 2144), and Remove button with inline confirm (lines 2151-2174). For plugins in `reconsent_required` state the acti
  - gap: Reconsent flow has no GUI button. Plugins stuck in `reconsent_required` can only be re-enabled via CLI. Right-click: Enable · Disable · View activity · Reconsent · Remove. All CLI calls (neoth plugin enable/disable/remov
  - build: Wrap each plugin row HorizontalLayout in a Rectangle with `pointer-event`, fire `callback plugin-context(string, length, length)` on SettingsView. SettingsView re-emits upward; AppWindow gains `plugin-ctx-open/id/x/y` + `ContextMenu` [Enable · Disable · View activity · Reconsent · Remove] in main.slint. Reconsent and Enable share the same
- [ ] **Presets list rows: right-click with Show Details / Export (missing from inline UI)** `settings.slint`
  - now: Each preset data row (settings.slint:4411) has Apply button (line 4451) and Delete button for non-builtin presets (line 4457). No way to view the full preset YAML body in the GUI. `neoth preset show <name>` exists in pre
  - gap: Show preset detail and Export preset have no GUI path. Right-click: Apply · Show details · Export · Delete. Show details calls `neoth preset show <name>` and could feed a detail card or toast; Export calls `neoth preset 
  - build: Wrap each `!p.is-header` preset row HorizontalLayout in a Rectangle with `pointer-event`, fire `callback preset-context(string, length, length)` on SettingsView. AppWindow gains `preset-ctx-open/name/x/y` + `ContextMenu` [Apply · Show details · Export · Delete] in main.slint. `preset-ctx-show` in panel_logic.rs calls `neoth preset show <n
- [ ] **Cluster peers in Settings tab: no interaction at all, missing Remove Peer** `settings.slint`
  - now: `for peer in root.cluster-peers` at settings.slint:3698 renders a read-only VerticalLayout with label, status text, and addr/rtt/last-seen text (lines 3698-3723). No TouchArea, no callbacks, no clicked handler anywhere i
  - gap: Zero interaction on cluster peer rows in the Settings/Cluster tab. Remove peer, copy pub_key, and ping are all absent. `neoth cluster remove <pub_key>` exists (cluster.rs:213). Note: ClusterPeerRow struct needs a `pub-ke
  - build: First add `pub-key: string` to ClusterPeerRow struct (settings.slint:70) and populate it in panel_logic.rs. Then wrap each peer row in a Rectangle with `pointer-event`, fire `callback cluster-peer-context(string, length, length)` (pub-key, x, y) on SettingsView. AppWindow gains `cluster-peer-ctx-open/key/x/y` + `ContextMenu` [Copy pub_key
- [ ] **WAL inspector rows: right-click per-row copy / export frame** `wal_inspector.slint`
  - now: WalEventItem (wal_inspector.slint:132) has `callback clicked(int)` (line 135) — single-click selects row for the detail pane. The global `copy-detail-clicked` on WalInspectorView (line 273) copies the currently-selected 
  - gap: No right-click. Per-row copy/export requires two gestures (click to select, then click the copy button). Right-click on any row should offer: Copy seq · Copy detail · Export this frame. `neoth wal export` (wal.rs:78) acc
  - build: Add `pointer-event` to WalEventItem's TouchArea (wal_inspector.slint:242 area) alongside existing `clicked`, detect right-up, fire `callback context-menu(int, length, length)` (seq, x, y). WalInspectorView re-emits as `callback wal-row-ctx(int, length, length)`. AppWindow gains `wal-row-ctx-open/seq/x/y` + `ContextMenu` [Copy seq · Copy d
- [x] **ADR browser (Architecture Decision Records)** `adr.rs` — ✅ STALE: built Wave G1 — NavBtn app_shell.slint:246 (nav=adr-browser), ProbeView main.slint:3696, handler main.rs:3975 (neoth adr list --output json).
  - now: adr.rs has list (JSON: number/title/path) and extract (parse ADRs from file/stdin). ZERO presence in any GUI file: no NavBtn, no panel, no nav-active condition. ADRs live in ~/.neoth/adr/ and represent the operator's own
  - gap: There is no GUI panel for browsing the decision log, viewing ADR titles and their file paths, or running the extract tool against a conversation file to capture a decision automatically.
  - build: Add an 'adr' NavBtn under KNOWLEDGE (complexity >= 1). Create adr.slint with a simple AdrRow {number: int, title: string, path: string} list (rows from neoth adr list --output json), sorted by number descending so newest decisions appear first. Wire refresh_adr() in main.rs following the refresh_wiki pattern: spawn neoth adr list --output
- [x] **Council routing weights inspection** `council.rs` — ✅ STALE: built — NavBtn app_shell.slint:256 (council-weights), ProbeView main.slint:3687, handler main.rs:3933.
  - now: main.slint:706 wires council tune fields (daily_usd_cap, max_calls, max_depth, selection_mode) to the Config settings tab with write-back callbacks. council.rs weights subcommand reads ~/.neoth/routing_weights.json and e
  - gap: Operators set the council tune parameters but cannot see the learned routing preferences (which topics steer toward which hemisphere) that actually drive model selection. The weights are the outcome of those settings app
  - build: Add a 'council-weights' NavBtn under INTELLIGENCE (complexity >= 2) in app_shell.slint. In main.slint, add an if block after the tweaks ProbeView block (line ~3651): if nav-active == 'council-weights': ProbeView { blurb/output/running/run-clicked wired to council_weights_output/running and on_council_weights_run_clicked }. In main.rs, add
- [x] **Background job monitor (ad-hoc detached jobs)** `jobs.rs` — ✅ STALE: built Wave G1 — NavBtn app_shell.slint:224 (bg-jobs), ProbeView main.slint:3714, handler main.rs:4059.
  - now: Automation panel (nav 'automation') renders CronView from cron.slint with full CRUD for jobs.yaml scheduled jobs. jobs.rs --bg flag lists detached background jobs from ~/.neoth/bgjobs/ (id, status: running/completed, exi
  - gap: Operators who launch long-running tasks via neoth jobs --run (cargo build, test suites, ingest runs) have no GUI view of running or completed jobs, their exit codes, or log tails — they must poll the CLI or check log fil
  - build: Append a Background Jobs sub-section to the existing CronView (cron.slint) below the scheduled jobs list, or add a BgJobRow {id, label, status, exit_code, log_tail} second tab. Wire refresh_bg_jobs() in main.rs: spawn neoth jobs --bg --output json, parse BgJobRow list, push VecModel. Add a 'Spawn job' form: command LineEdit + label LineEd
- [ ] **MCP tools inspector and tool-call surface** `mcp.rs`
  - now: main.slint:3590 has a ProbeView for neoth mcp list (server registry, read-only string). mcp.rs has: tools <server> (spawns the MCP binary, runs tools/list, renders catalogue with schema); call <server> <tool> --args '{..
  - gap: Operators cannot inspect a configured MCP server's available tools, their schemas, or invoke a tool to test an integration without dropping to the CLI. Debugging an MCP integration requires the terminal.
  - build: Extend the existing MCP ProbeView (main.slint:3590) into a two-pane McpView: a server list on the left (typed McpServerRow {id, enabled, launcher} from neoth mcp list --output json), and on the right a server-detail pane with a 'Inspect tools' button (on_mcp_tools_clicked(server) -> neoth mcp tools <server>, result in mono text block) and
- [ ] **Schema migration panel (migrate list / run)** `migrate.rs`
  - now: main.rs:1339 calls which_neoth_migrate() during welcome screen to detect prior AI tool data — this uses a separate neoth-migrate binary for data migration, not the neoth migrate subcommand. The actual neoth migrate {list
  - gap: After a daemon update that bumps SCHEMA_VERSION, operators have no GUI way to inspect which migrations have run or trigger a manual migration pass without restarting the daemon. The list subcommand is safe read-only diag
  - build: In settings.slint, add a 'Database schema' Card below the Backup section (near line 4898). Add a ProbeView-style read block: 'Migration history' button -> on_settings_migrate_list_clicked() in main.rs shells neoth migrate list --output json, shows result in mono block. Add a 'Run pending' button with a dry-run-first pattern: on_settings_m
- [ ] **Mode registry browser (QM-3 interaction modes)** `mode.rs`
  - now: Skills tab in settings.slint shows SkillRow entries (id/description/enabled/keywords/tags) with enable/disable. mode.rs has: list (all modes sorted by id, JSON: id/spectrum/oversight/output_contract/trigger_phrases), sho
  - gap: Operators can see and toggle skills but cannot browse registered interaction modes, view their trigger phrases, or test which mode a given message would activate. Mode routing is the layer between skill enablement and ac
  - build: Add a 'modes' NavBtn under INTELLIGENCE (complexity >= 2). Create modes.slint with a ModeRow {id, spectrum, oversight, trigger_count} list from neoth mode list --output json. Clicking a row opens an inline detail pane showing full shape via neoth mode show <id> --output json. Add a 'Test match' form: a message LineEdit + Match button wire
- [ ] **Task dependency data exists in DB but is completely invisible in the GUI** `store.rs:113`
  - now: idx_kanban_task_dep table (store.rs:113-122) with dep_id, task_id, depends_on_task_id, created_ns exists and has indexes. TaskDep struct (types.rs:293-306) is defined and serializable. The decomposer and dispatcher refer
  - gap: A task in Blocked state may be blocked because a dependency task is not yet Done — or it may be blocked because the worker failed. The operator cannot distinguish these two Blocked causes from the board. Linear's 'blocks
  - build: Add 'blocks-count: int, blocked-by-count: int' to KanbanTaskRow (settings.slint:30). Populate in main.rs board-refresh by calling store::list_deps_for_task for each task (or a batch SQL join). Render a small pill badge on KanbanTaskCard when blocks-count > 0 ('blocks N') in Theme.warning color. In the detail pane (settings.slint:4060), ad
- [ ] **No real-time WAL push to board — requires manual Refresh button** `store.rs:749`
  - now: The board is populated by a one-shot subprocess call to 'neothd kanban list/show' triggered by kanban-refresh-clicked (settings.slint:749). The code comment at settings.slint:747 explicitly says 'Live-tail (Pick #8 step 
  - gap: While a worker is running, the operator staring at the board sees nothing move until they click Refresh. This defeats the purpose of having a live board during an autonomous coding session. Linear and GitHub Projects upd
  - build: In main.rs, after opening the WAL reader, spawn a tokio task that tails the WAL file for new frames. On 0x73 (KANBAN_STATUS_CHANGED) or 0x77 (KANBAN_TASK_PROGRESS), call a Slint weak-handle callback that triggers a targeted re-fetch of the affected task row from the store and mutates only the relevant kanban-* array entry (or triggers a l

## LOW · needs-rust · CODEX-ZONE

- [ ] **Agents view: completely non-interactive, missing click-to-detail and context menu** `agents_view.slint`
  - now: AgentCard (agents_view.slint:161) is a display-only card: no TouchArea, no callbacks, no clicked handler at all. The AgentsView (agents_view.slint:324) exposes no per-agent callbacks. `neoth agents show <name>` and `neot
  - gap: Agents panel is fully read-only — there is no click-to-inspect and no right-click. At minimum, clicking an agent card should show the last run record. Right-click: Show detail · Copy name · Copy model. No kill/pause subc
  - build: Add a TouchArea to AgentCard body with `clicked => { root.agent-clicked(agent.name); }` and `pointer-event` for right-click firing `callback context-menu(string, length, length)`. AgentsView re-emits as `agent-ctx(name, x, y)`. AppWindow gains `agent-ctx-open/name/x/y` + `ContextMenu` [Show detail · Copy name · Copy model] in main.slint. 
- [ ] **Loop run history: no right-click, missing Copy ID / Export** `loop.slint`
  - now: `run-touch := TouchArea { clicked => { root.run-selected(run.id); } }` at loop.slint:173. Selection is the only action. LoopPanel has `callback run-clicked(string)`, `callback kill-clicked()`, `callback refresh-clicked()
  - gap: Right-click on a loop run should offer Copy run ID and Export run log, but loop_cmd.rs has no export/delete subcommand yet. The GUI right-click cannot be fully wired until those CLI subcommands exist. Copy run ID to clip
  - build: Add `pointer-event` to `run-touch` in loop.slint:173 alongside existing `clicked`, fire `callback run-context(string, length, length)`. LoopPanel re-emits as `loop-run-ctx(id, x, y)` on AppWindow. AppWindow mounts `ContextMenu` [Copy run ID · Export run log (coming soon)] in main.slint. Only Copy run ID is wirable immediately via a clipbo
- [ ] **Card face information density too low — only title and hemisphere badge visible** `settings.slint:245`
  - now: KanbanTaskCard (settings.slint:245-332) renders: a 2px color bar on the left edge (accent by status), the title text (word-wrap, up to 2 lines via max-height), and a hemisphere badge pill. KanbanTaskRow struct (settings.
  - gap: At a glance the operator cannot tell whether a card in Review has a passing test suite (auto-promotable) or a failing one (needs manual intervention), whether a Blocked card has a patch attached (retrievable) or nothing 
  - build: Add 'test-ok: bool, patch-ready: bool, has-parent: bool' to KanbanTaskRow (settings.slint:30). Populate in main.rs board-refresh from KanbanTask.test_summary.all_green() and patch_path.is_some() and parent_task_id.is_some(). In KanbanTaskCard (settings.slint:245), add a status icon row below the title: a green checkmark when test-ok, a pa
- [ ] **WAL timeline buckets: no right-click, missing Filter-to-bucket / Export bucket** `wal_timeline.slint`
  - now: BucketColumn (wal_timeline.slint:120) has `callback clicked(int)` (line 128), wired as `bucket-selected(int)` on WalTimelineStrip (line 284). Single-click selects the bucket (highlighting it). No pointer-event, no contex
  - gap: No right-click. Filter WAL inspector to this time range and Export time range bucket are both absent. CLI: `neoth wal export` with `--after/--before` time filters exists (wal.rs). This is a forensic power-user feature us
  - build: Add `pointer-event` to BucketColumn's col-ta TouchArea (wal_timeline.slint:253), detect right-up, fire `callback context-menu(int, length, length)` (bucket index, x, y). WalTimelineStrip re-emits as `wal-bucket-ctx(idx, x, y)`. AppWindow gains `wal-bucket-ctx-open/idx/x/y` + `ContextMenu` [Filter WAL to this hour · Export this bucket] in 