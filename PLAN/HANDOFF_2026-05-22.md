# NEOTH — Session 20 Handoff (2026-05-22)

**Repo**: `<your-workspace>/AGENTER`
**Public mirror**: `github.com/The-Geek-Freaks/NEOTH`
**Branch**: `main`
**Last commit**: `2d42b07` (Session 20 end)
**Tests**: 3097/3097 daemon, 31/31 GUI. clippy --tests -D warnings clean.

---

## What shipped this session

Mega-session with three explicit operator-driven phases:

### Phase A — R4-P1 + backlog top-20 (early session)
Picks 1-8 of the 8-pick R4 reviewer P1 list closed. Plus dependabot
critical patched (`aquasecurity/trivy-action@0.28.0` → 0.35.0,
supply-chain compromise). Notable: Slack + WhatsApp inbound LIVE
in `cli/serve` (was OUTBOUND-ONLY); NEOTH-AIO hard rule registered
in memory.

### Phase B — 7 picks listed by operator (mid session)
1. QM-9 Phase 3 GUI dashboard tile (visible "Last 24h usage" Card)
2. QM-10 Phase 2.5 breaker in MCP loop + council debate dispatch
3. QM-8 Phase 1.5 preset apply merges into freedom.yaml (atomic)
4. QM-2 Phase 2 C-family comment-stripping + code_map_edges SQLite
5. R-02 Phase 2 seed_with_dreams + `neoth recall --include-dreams`
6. R-2 Phase 1+2 Hyperswarm crypto + DHT bootstrap shape
7. R-3 Phase 3b Hysteria SOCKS5 helpers + serve refactor

### Phase C — backlog (late session, the explicit "von oben nach unten")
1. Cluster Phase 1 SPEC + primitive ratified via 4-agent council
   (Q1 reuse / Q2 raw-pubkey / Q3 require-consent / Q4 default-ON)
2. 3 security fixes from auditor (HMAC key-vs-data, length-
   delimiters, ClusterKey Drop-zeroize)
3. Action::ClusterPeerPairing variant + autonomy-level resolver arms
4. Cluster Phase 4 registry (`cluster::registry` + `~/.neoth/
   cluster.yaml` + CLI confirm/revoke/list/enable/disable)
5. Doctor `cluster registry` check (stale-peer warning at 14d)
6. Cluster Phase 2 mDNS announcer/listener (mdns-sd crate +
   `_neoth._udp.local.` service, TXT v=1 schema, sanitize_for_dns)
7. Cluster Phase 3 Tailscale magic-DNS scan (`tailscale status
   --json` shell-out + parallel TCP probe)
8. Cluster Phase 5+6 SPECs (Hysteria-shared relay + gossip state-
   sync, both multi-week)
9. Wizard cluster step (Slint UI + Q4-verbatim copy + opt-out checkbox)
10. WAL 0xE6/0xE7 reservation + payload spec + band-check assertions
11. cluster::policy + announce_on_untrusted_wifi flag + SSID
    lookup helper (Windows netsh / macOS networksetup / Linux iwgetid)
12. cluster::audit_sidecar bridge + serve-side ingester task (5s tick)
13. WizardSnapshot.cluster_discovery_disabled → freedom.yaml
14. R-2 Phase 3+ SPEC (DHT UDP dialer / NOISE-IK / Hypercore
    replication, multi-week with port-as-you-go vs Node-subprocess
    decision deferred)
15. R-02 Phase 3 SPEC (LLM clustering, blocked on Day-14b)
16. Channel-flapping detector in doctor (20% err-rate over 5+ samples)
17. Persisted circuit-breaker state across restarts (Phase 3)
18. GUI Apply-active-preset button + click handler

### Test counter

Session start: 2868 daemon, 21 GUI.
Session end: **3097 daemon (+229), 31 GUI (+10)**.

---

## State of the codebase

### Files added this session
- `SRC/neothd/src/channels/keet_crypto.rs`
- `SRC/neothd/src/channels/keet_dht.rs`
- `SRC/neothd/src/cluster/discovery.rs`
- `SRC/neothd/src/cluster/registry.rs`
- `SRC/neothd/src/cluster/mdns.rs`
- `SRC/neothd/src/cluster/tailscale.rs`
- `SRC/neothd/src/cluster/policy.rs`
- `SRC/neothd/src/cluster/audit_sidecar.rs`
- `SRC/neothd/src/code_map/graph.rs`
- `SRC/neothd/src/coding/...` (earlier in session)
- `SRC/neothd/src/cli/preset.rs`
- `SRC/neothd/src/cli/usage.rs`
- `SRC/neothd/src/config/presets.rs`
- `SRC/neothd/src/daemon/usage_log.rs`
- `SRC/neothd/src/daemon/dreaming.rs`
- `SRC/neothd/src/providers/circuit_breaker.rs`
- `PLAN/SPEC_cluster_auto_discovery_2026-05-22.md` (ratified)
- `PLAN/SPEC_cluster_phase5_hysteria_relay_2026-05-22.md`
- `PLAN/SPEC_cluster_phase6_gossip_state_sync_2026-05-22.md`
- `PLAN/SPEC_R2_phase3_dht_noise_replication_2026-05-22.md`
- `PLAN/SPEC_R02_phase3_llm_clustering_2026-05-22.md`

### Cargo.toml dep changes
- Added: `mdns-sd = "0.11"` (cluster Phase 2)
- Bumped: `aquasecurity/trivy-action@0.35.0` in `.github/workflows/security.yml`

### Hard rules registered in memory
- `neoth_features_default_on_runtime_toggle.md`
- `neoth_aio_cross_platform.md`
- `neoth_claude_cli_tmux_mandatory.md`
- `neoth_progress_md_update_rule.md` (every shipped item updates PROGRESS.md same turn)
- `neoth_public_release_safety.md`

---

## Multi-week / multi-session items still open

### R-2 Hyperswarm port — operator decision pending
**SPEC**: `PLAN/SPEC_R2_phase3_dht_noise_replication_2026-05-22.md`

Two paths to ratify:
1. **Port-as-you-go** (~12 weeks, AIO-compliant)
   - Phase 3 DHT UDP dialer (2-3 weeks)
   - Phase 4 NOISE-IK handshake (1-2 weeks)
   - Phase 5 Hypercore replication (3-4 weeks)
2. **Embed Node.js subprocess** (~2 weeks to Keet interop)
   - Bundles `hyperdht` + `udx` + `hypercore` JS packages
   - Violates AIO hard rule (NOOB-UX-6 Node install friction)
   - Backward step from the in-binary promise

Default leans port-as-you-go but the timeline matters for the
"Keet works on day one" feature pitch.

### R-02 Phase 3 LLM clustering — blocked
**SPEC**: `PLAN/SPEC_R02_phase3_llm_clustering_2026-05-22.md`
**Blocker**: Day-14b forward-pass + sampling-loop (in-process
Qwen3-Q8 inference via candle 0.8). Phase 1 of Day-14b shipped
(accelerator-detect + InferenceTopology + 3-hemisphere wiring),
forward-pass + sampling-loop NOT yet. See memory
`neoth_d14b_phase1`.

### Cluster Phase 5 — Hysteria-shared relay (~6 weeks)
**SPEC**: `PLAN/SPEC_cluster_phase5_hysteria_relay_2026-05-22.md`

Needs operator-side Hysteria config change + a separate relay
daemon (`neoth-relay`) co-located with Hysteria. 3 open questions:
co-location vs standalone, rate-limit policy, federation mesh.

### Cluster Phase 6 — Gossip state-sync (~6-10 weeks)
**SPEC**: `PLAN/SPEC_cluster_phase6_gossip_state_sync_2026-05-22.md`

WAL frame replication via vector-clock gossip + memory tier
replay + JSONL append-stream + BudgetToken Raft consensus.
3 open questions: replay budget, per-channel filter, opt-out tags.

### Day-14b forward-pass — blocker for R-02 Phase 3
Per memory `neoth_d14b_phase1` Phase 1 done (candle clean
MSVC build, accelerator-detect CUDA/Metal/OpenVINO/CPU,
InferenceTopology single/triplet/custom, 3 hemispheres,
Hermes/OpenClaw stub, wizard step 5b). **Forward-pass +
sampling-loop is the open work** to unblock R-02 Phase 3 LLM
clustering + the skill-router Stage-2 embedding re-rank.

---

## Operator decisions pending

1. **R-2 port strategy** (port-as-you-go vs Node-subprocess) — see SPEC
2. **R-02 Phase 3 cadence** (cron 03:00 vs `neoth dream now`) — see SPEC
3. **R-02 Phase 3 embedding model** (Qwen3-Q8 vs BGE-small-en) — see SPEC
4. **R-02 Phase 3 privacy boundary** (include channel ingress in clustering?) — see SPEC
5. **Cluster Phase 5 relay co-location** (with Hysteria vs standalone) — see SPEC
6. **Cluster Phase 5 rate limit** (peers per cluster_key cap) — see SPEC
7. **Cluster Phase 5 federation** (multi-relay mesh?) — see SPEC
8. **Cluster Phase 6 replay budget** (>30d offline peer behaviour?) — see SPEC
9. **Cluster Phase 6 channel-ingress filter** (replicate Telegram/Slack/WA frames?) — see SPEC
10. **Cluster Phase 6 opt-out tag** (per-event "do not gossip" tag?) — see SPEC

---

## Build / test / lint state

- `cargo build -p neothd --bin neothd` → clean
- `cargo test -p neothd --bin neothd` → 3097/3097 (2 ignored)
- `cargo test -p neothd-gui` → 31/31
- `cargo clippy -p neothd --bin neothd --tests -- -D warnings` → clean
- `cargo audit` → 5 unmaintained-warnings (bincode/number_prefix/paste/
  rustls-pemfile/xsalsa20poly1305), no CVEs

### Known flaky test
`coding::dispatcher::tests::dispatch_session_with_apply_*` (5 tests)
intermittently fail under `cargo test`'s default parallel run with
`program not found` errors. Always pass in isolation
(`cargo test coding::dispatcher`). Race condition in temp-dir +
parallel `git` subprocess. Not a blocker — workaround is to rerun.

---

## Hard rules to carry forward

From `~/.claude/CLAUDE.md` + memory notes (every session continues
to honour these):

1. **Verify before claiming done** — run the command, read the output, then claim
2. **No SendUserMessage** — it renders unreadably in some UIs; reply in chat text
3. **NEVER reboot Cube** (100.68.210.50) — physical power button only
4. **Features default-ON in shipped release + runtime toggle + noob wizard explains**
5. **NEOTH AIO cross-platform** — every dep ships in-binary OR auto-installs headless; operator never opens npm/brew/apt manually
6. **claude-cli requires tmux** — subprocess --print mode is broken, tmux warm session is the only working path
7. **NEOTH self-contained** — every feature MUST be inside the binary or installed BY the wizard
8. **Public release safety** — QUELLEN/+RECON/+SRC/target/+stray `nul` MUST be excluded from public push
9. **PROGRESS.md update same turn** — every shipped item flips its checkbox before the session moves on
10. **NEOTH design v1.1 is the norm** — `00_DESIGN_v1.1_FINAL.md` + `SPEC_*.md` are authoritative; older docs may carry superseded ideas
11. **Slash commands + GUI settings parity** — every CLI command pairs with a GUI settings panel entry
12. **GUI mode-selection first + settings parity + mine QUELLEN** — startup shows mode-selection screen; all operator-tweakable settings have GUI + CLI parity

---

## Single-session bites left at session end

These fit a 1-2 hour next-session start:

- `neoth cluster discover` could also surface the `policy::evaluate`
  verdict before scanning (skip-and-explain when policy=No)
- Doctor `mdns announcer` check (mirror of `tailscale status`): show
  whether the announcer is currently running + which SSID it's on
- `neoth cluster confirm --interactive`: pick from the discover
  output without copy-pasting pub_keys
- `freedom.yaml::cluster.listen_port` config knob (Phase 2 mDNS +
  Phase 3 Tailscale TCP probe both want this)
- Slint settings panel entry for cluster (mirror the wizard step
  for already-onboarded operators who change their mind)

---

## Handoff prompt (paste this to next session)

```
NEOTH continuation session. Repo: <your-workspace>/AGENTER.
Main branch, last commit 2d42b07. Tests: 3097/3097 daemon, 31/31 GUI,
clippy --tests -D warnings clean.

Read PLAN/HANDOFF_2026-05-22.md for the full session summary. Active
hard rules (CLAUDE.md + memory): verify before claiming done, no
SendUserMessage, features default-ON + runtime toggle + noob wizard,
NEOTH AIO cross-platform (every dep ships in-binary or auto-installs
headless), claude-cli needs tmux, public release excludes QUELLEN/
RECON/SRC/target, every shipped item updates PROGRESS.md same turn.

Active operator-decisions pending (10 — see HANDOFF doc):
  - R-2 port-strategy (port-as-you-go vs Node-subprocess)
  - R-02 Phase 3 (4 design questions on cadence/model/privacy)
  - Cluster Phase 5 (3 design questions on relay co-location/rate
    limit/federation)
  - Cluster Phase 6 (3 design questions on replay budget/channel
    filter/opt-out tags)

Multi-week items blocked or pending:
  - R-2 Phase 3+ Hyperswarm port (12 weeks port-as-you-go)
  - R-02 Phase 3 LLM clustering (blocked on Day-14b forward-pass)
  - Cluster Phase 5 Hysteria relay (6 weeks)
  - Cluster Phase 6 gossip state-sync (6-10 weeks)
  - Day-14b forward-pass + sampling-loop (unblocks 2 downstreams)

Single-session bites ready to pick up:
  - cluster discover surfaces policy verdict
  - doctor mdns-announcer running-state check
  - cluster confirm --interactive
  - cluster.listen_port config knob
  - Slint settings panel cluster section

Build env: vcvars64 wrapper at C:\Temp\cargo-wrap.bat (handles
ucrt INCLUDE/LIB patch). Slint UI lives in SRC/neothd-gui/ui/.
Daemon binary is SRC/neothd/.

Branch is at parity with origin; no local pending commits. Use
`git push origin main` after each commit. Dependabot critical
already patched (trivy-action@0.35.0).

What would you like to ship next?
```

---

## Final commit graph this session (most recent 24)

```
2d42b07 feat(cluster): wire 3 single-session bites — SSID + sidecar + wizard
4b13a82 docs(r02): SPEC for Phase 3 LLM clustering (blocked on Day-14b)
9da4ba4 docs(r2): SPEC for Phase 3+ DHT/NOISE-IK/Hypercore replication
d5535bb feat(gui): cluster discovery wizard step (Q4 ratified Design B)
cfc992b feat(cluster): WAL 0xE6/0xE7 cluster band + announce policy + Phase 5/6 SPECs
bb2ddfa feat(cluster): Phase 3 Tailscale magic-DNS peer enumeration
b515720 feat(cluster): Phase 2 mDNS announcer/listener + 4 clippy + product-truth fixes
69987fd feat(doctor): cluster registry visibility surface (Phase 4 follow-on)
7e10e52 feat(cluster): Phase 4 — registry + CLI surface (list/confirm/revoke/enable/disable)
4c75fa2 feat(cluster): ratify Phase 1 SPEC + apply 4-agent verdicts
1d71422 feat(cluster): Phase 1 cluster auto-discovery primitives + SPEC
2e23288 feat(keet): R-2 Phase 2 — DHT bootstrap list + announce packet shape
55b3d2c feat(keet): R-2 Phase 1 — Hyperswarm pairing crypto primitives
4ddf1b6 feat(doctor): channel-flapping detection over last 24h of usage_log
f1eef6d feat(usage): instrument MCP loop + council debate with usage_log
248f647 feat(gui): QM-8 Phase 2.5 — Apply active preset button + click handler
97e0cf6 feat(chat): wire breaker + usage_log into streaming chat path
cabd1bf feat(gui): QM-8 Phase 2 — saved presets dashboard tile
604a130 feat(gui): QM-9 Phase 3+ — usage tile auto-refreshes every 60s
ac1fd44 feat(breakers): QM-10 Phase 3 — persisted state across daemon restarts
a33786d feat(code-map): QM-2 Phase 2.5 — Python/Shell # comment stripping
0f15b31 refactor(serve): R-3 Phase 3b — use install_as_process_proxy helper
65e1a5d docs(progress): Session 20 final scoreboard for the 7-pick batch
2236a49 feat(transport): R-3 Phase 3b — Hysteria SOCKS5 proxy wire-through helpers
```

Earlier in session (24+ earlier commits between session start and
the picks above): R4-P1 routing, Slack+WhatsApp inbound, OnSessionStart
hook firing, NOOB-UX-6 doctor probes, QM-10 + QM-9 + QM-2 + QM-8 phase 1s,
R-02 Phase 1, R-3 Phase 3b, ratified-via-4-agents Phase 1 cluster
primitives, etc.

---

End of handoff.
